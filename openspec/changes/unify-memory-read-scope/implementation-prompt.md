# Claude Code 执行 Prompt

请在当前 VaneHub AI 仓库完整实施 `openspec/changes/unify-memory-read-scope`：先读取 `AGENTS.md`、本变更的 proposal/design/tasks/acceptance 和全部 specs，核对当前 HEAD 与基线及 `strengthen-governed-cross-session-memory` 等活动变更的重叠，保留已有工作并严格校验后按任务依赖实施，不要停留在分析。目标是保留共享记忆存储，复用现有 read/global-memory-access、会话模式、scope/audience，由原生解析的同一可信 MemoryReadContext 约束索引注入、正文选择、recall 和 Context Engine；禁止新增平行 readScope 设置、把作者当权限或把前200条refs当完整授权集。将snapshot解析前移，修复Context Engine旁路和pinned_bodies误用compatibility视图，FTS/向量必须在排名与top-k前按完整资格域过滤，交付摘要和正文前核对权威ID/revision/hash及scope/audience；保留每generation政策快照语义、正常检索降级与生成继续。正文选择使用immutable ID，缓存/seat/工作区/重注入不能复用旧授权；owner管理与后台索引保持独立类型边界，不能简单放宽compatibility或自动扩大远端embedding正文外发。同步真实会话预览、Web/native policy继承与空池状态、Tauri/Web合约和中英文文档，遵守DDD、React service边界与统一脱敏日志。使用真实临时v2文件、SQLite/FTS和固定embedding/provider fixture验证完整正反路径，重点覆盖project-only正文、temporary/read-disabled零memory调用、200条之外合法召回、越权top-k挤占、同名ID、元数据变化和并发seat，并执行tasks及当前AGENTS要求的检查；仅勾选实际完成且验证过的任务。常规可逆工作自主推进，持续汇报进展，最终说明改动、实际测试、未执行项和兼容边界；不覆盖其他变更或修改归档绕过检查，不自动归档、commit、push、merge或发布。
