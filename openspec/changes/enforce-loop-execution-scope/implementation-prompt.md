# 实施提示词

请在当前 VaneHub AI 仓库实施 `openspec/changes/enforce-loop-execution-scope`。先读取仓库 `AGENTS.md`、本变更的 `proposal.md`、`design.md`、`tasks.md`、`acceptance.md` 和全部 `specs/*/spec.md`，核对当前 HEAD 与基线 `75f23d81406bdc1fd4d6b4d5a41fbf90e395c4fc` 的差异及活动变更 `extend-cli-providers-with-acp`，保留已有代码和规范工作，严格校验本变更后按任务依赖实施。目标是让 Loop 的用户路径配置成为真实执行上限，并修复所有 ACP 会话中 file.read 的 Ask/故障分支；不能只改提示词、CLI 参数、cwd 或 Git diff，也不能禁用所有 Loop 后宣称完成。默认要求执行前完整约束；未覆盖的 CLI/脚本/MCP 必须阻断或在用户逐次明确选择的产物审计模式中准确披露，禁止自动降级，Verifier 始终只读。沿用统一权限、审批事务、DDD、operations/logs 和 React service→Tauri/Web adapter 边界；处理真实资源身份、链接竞态、跨 run 批准、当前撤权、完整产物证据、接受 CAS 及迁移恢复。不要修改归档规范或其他活动变更来绕过验证，不自动归档、merge、push 或发布。逐项实现并使用真实临时文件 sentinel 验证允许/拒绝/读取内容是否发生，运行 tasks.md 及当前 AGENTS.md 要求的检查，只有完成且有证据的任务才能勾选。持续汇报已完成工作、发现及具体阻塞；常规可逆实现不反复请求许可。最终报告改动、实际测试结果、未通过或未执行项及各平台真实能力限制，不把规范校验、mock 测试或未覆盖能力描述成已完成的运行时安全保证。
