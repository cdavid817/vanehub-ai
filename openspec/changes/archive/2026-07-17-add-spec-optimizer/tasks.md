## 1. Spec Optimizer Skill

- [x] 1.1 Create the project-local `spec-optimizer` Codex skill with read-only scope and archive exclusion.
- [x] 1.2 Define duplicate, clustering, budget, mapping, coverage, and diff outputs.

## 2. Verification

- [x] 2.1 Validate the skill structure and run a read-only sample analysis against the main spec corpus.
- [x] 2.2 Run `openspec validate add-spec-optimizer --strict` and `openspec validate --specs --strict`.
