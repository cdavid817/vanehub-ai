## 0. Baseline, Ownership, and Failing Tests

- [x] 0.1 Read `AGENTS.md`, `openspec/config.yaml`, `openspec/project.md`, every delta spec in this change, and the existing affected capability specs before editing implementation code.
- [x] 0.2 Run `openspec validate upgrade-session-workspace-evidence-console --strict` and resolve change-document errors before implementation.
- [x] 0.3 Inventory the current frontend service methods, Tauri/Web session-workspace adapters, Rust commands, SQLite migrations, Tauri events, and tests for Changes, Terminal History, Documents, Files, Shell, Logs, Traces, Report, Review, Usage, and Basic Info.
- [x] 0.4 Record the current command names and serialized DTO shapes that must remain compatible during the migration.
- [x] 0.5 Add or update architecture tests proving the change adds no Rust bounded-context directory and cross-context access uses published APIs or explicit ports.
- [x] 0.6 Add failing frontend contract tests for the native `remote` Shell descriptor, effective seat query propagation, hunk-scoped decision mutation, and loaded-log preservation after a load-more failure.

Tasks 0.7, 0.8, and 0.9 are deferred test gates. Each one is a test that can only pass once the
group that owns its subject exists, so landing it here would leave the suite red for every
intervening group rather than proving anything. Each is instead due immediately before the task
named below, and blocks only that task.

- [x] 0.7 Add failing Rust tests demonstrating stable newest-first keyset pagination while newer evidence/log rows are inserted. Deferred test gate, split by subject: the evidence pagination fixture is due before 3.9, and the log pagination fixture is due before Group 8. It does not block Groups 2-7. Tick 0.7 only once both fixtures exist and pass.
- [x] 0.8 Add a controlled repository fixture in which a generated review patch can be checked with `git apply --check`. Deferred test gate, due before 13.7, which is the task that first renders a patch to check. It does not block Groups 2-12. `test_support/git_patch_fixture.rs` builds a real repository with a known base commit and runs the same check Git runs for a patch arriving from anywhere else; refusals carry Git's own message, because "it did not apply" is equally true of a corrupt patch, a stale one, and one for a file that is gone. The harness proves itself against a hand-written patch rather than against the renderer it exists to check — including a hunk header that miscounts by one, which is what a rendering-only assertion cannot see. Extending `is_test_source` to cover `test_support/` was needed for ARCH-NATIVE-004: `lib.rs` gates that module under `#[cfg(test)]`, which a per-file visitor cannot see — the blind spot the rule's own comment describes.
- [x] 0.9 Establish test builders for run/trace/span/operation/seat/command/file correlations and complete/indexing/partial/unavailable coverage states. Deferred test gate, due before 3.1, which is the task that first defines the correlation and coverage types these builders construct.

## 1. Immediate Contract and Correctness Fixes

- [x] 1.1 Replace the string-only frontend `ShellCapability` union with the discriminated `ShellRuntimeDescriptor`, including `native`, `remote`, `simulated`, and `unavailable` variants.
- [x] 1.2 Map the existing Rust local and remote Shell results to the new descriptor without changing the current create-shell command name during the transition.
- [x] 1.3 Update Tauri and Web/mock serialization fixtures and contract tests for every Shell runtime descriptor variant.
- [x] 1.4 Pass the selected seat id into Terminal History, Shell, and Logs service queries according to `tab-scope.ts`.
- [x] 1.5 Hide or disable the workspace seat switcher for session-scoped tabs and add an accessible explanation when a tab has no seat scope.
- [x] 1.6 Add tests proving a multi-Agent seat change alters seat-scoped query keys/results and does not reset session-scoped tabs.
- [x] 1.7 Add a dedicated `setCodeReviewHunkDecision` service method and transport DTO; stop routing hunk Accept through `setCodeReviewDecision`.
- [x] 1.8 Implement a Web/mock hunk decision mutation that changes only the selected fixture hunk and labels the mutation simulated.
- [x] 1.9 Add review tests proving hunk Accept leaves the review decision, Git index, and working tree unchanged.
- [x] 1.10 Refactor Logs state so initial-load failure may show a blocking error, but refresh/load-more/live failures preserve already loaded entries and show an inline retry state.
- [x] 1.11 Attach error handling to Shell input and resize promises; display typed connection/process failure instead of silently discarding rejected operations.
- [x] 1.12 Split the current Copy Diff action into Copy Displayed Lines and a disabled placeholder for Copy Standard Patch until Task Group 13 implements the backend operation.

## 2. Frontend Evidence Types, Service Boundary, and Runtime Adapters

- [x] 2.1 Add `src/types/session-workspace-evidence.ts` with branded/validated ids, fidelity, status, coverage, cursor-page, evidence-scope, target, record, summary, and report DTOs.
- [x] 2.2 Add Zod transport schemas for new discriminated unions, opaque cursors, evidence notices, Shell frames, workspace capabilities, and report coverage.
- [x] 2.3 Add `SessionWorkspaceEvidenceService` as a focused frontend service interface and compose it through the existing application service injection path.
- [x] 2.4 Add Tauri adapter methods for evidence summary, record pages/details, evidence subscription, and session-run reports; keep all `invoke()` and native event APIs outside React. Group 2 implements only the injectable Tauri transport client, its serialization, schemas, and fixture transport. No method may call a command that is not yet registered: the production binding returns a stable typed unavailable reason code until its command exists. Evidence native methods are activated in 3.15; report native methods are activated in 10.8.
- [x] 2.5 Add deterministic Web/mock implementations with seeded ids, monotonic sequences, bounded pages, and explicit simulated side-effect metadata.
- [x] 2.6 Add one shared contract-conformance suite and run it against both Tauri serialization fixtures and the Web/mock implementation. In Group 2 the suite runs against the Web/mock implementation and the Tauri fixture transport. The same suite is re-run against the native evidence cases in 3.15 and against the native report cases in 10.8, so activation is proven by the cases that already exist rather than by new ones written after the fact.
- [x] 2.7 Define centralized evidence query keys so session, seat, run, trace, span, operation, command, path, filters, and cursor cannot be omitted accidentally.
- [x] 2.8 Keep production service and adapter files within the project line-size rule by extracting evidence, Shell, log, and workspace-inspection clients as focused modules.
- [x] 2.9 Add synchronized locale keys for coverage states, fidelity, evidence record kinds, reason codes, and cross-panel actions in every registered locale.

## 3. Execution Evidence Domain, Schema, Repository, and API

- [x] 3.1 Add evidence ids, correlation, kind, status, safe payload, redaction receipt, coverage, and domain errors under `execution_observability/domain/`.
- [x] 3.2 Enforce maximum safe-payload size, allowlisted fields, valid status/kind combinations, required session id, and fidelity invariants in domain constructors.
- [x] 3.3 Add an application `RecordExecutionEvidence` use case and query use cases for summary, record page, record detail, and correlation counts. Correlation counts are served through the record-detail query rather than a separate entry point: the counts and the record come from one store read, so a caller cannot receive a record whose related counts were computed against a different moment. A standalone pass-through was removed because nothing called it and an uncalled query is one nobody can vouch for.
- [x] 3.4 Add narrow application ports for evidence repository, clock, id generation, redaction validation, post-commit notice publication, and gap diagnostics.
- [x] 3.5 Add an additive SQLite migration for `execution_evidence_events`, projection tables, coverage/gap metadata, and required indexes.
- [x] 3.6 Implement atomic event insert plus monotonic projection update; publish notices only after the transaction commits.
- [x] 3.7 Enforce idempotency through `(source_context, source_event_id)` and treat identical duplicates as success.
- [x] 3.8 Reject conflicting duplicate source ids, preserve the original row, mark affected coverage partial, and emit one rate-limited redacted diagnostic.
- [x] 3.9 Implement opaque query-bound keyset cursors using timestamp, sequence, id, version, and filter fingerprint.
- [x] 3.10 Reject a cursor reused with different filters as `cursor_filter_mismatch` without returning an unstable page.
- [x] 3.11 Implement deterministic projection replay from journal events and a test-only projection reset/rebuild path.
- [x] 3.12 Add retention maintenance aligned with configured execution-timeline retention; delete expired projection rows consistently without per-event scans.
- [x] 3.13 Publish the narrow evidence recorder/query contract through `execution_observability::api` and keep repository/infrastructure modules private.
- [x] 3.14 Add Tauri commands and command-safe error mapping for evidence summary, record list, record detail, and subscription bootstrap data.
- [x] 3.15 Register new commands in the grouped command registry and add serialized DTO compatibility tests. This activates the evidence native methods added in 2.4: the production Tauri binding stops returning unavailable and invokes the registered commands, and the 2.6 conformance suite runs against those native cases. Report native methods stay typed unavailable until 10.8.

Tasks 3.13-3.15 are implemented and their focused tests pass, but their final verification depends
on the recorder being reachable from a real production caller. Until a producer publishes, the
whole write half of the journal -- the recorder, its ports, the notice publisher, the redaction
gate, and the encoding that feeds them -- is syntactically unreachable in the library build, and
`cargo clippy --workspace --all-targets -- -D warnings` fails on it. That is an accurate report of
the code's state, not a lint artefact, so it must be resolved by connecting a caller rather than by
silencing the lint.

Tasks 4.1-4.3 may therefore be implemented before 3.13-3.15 are ticked, and all six ticked together
once the bridge is live and clippy is clean. The exception covers 4.1-4.3 only: it does not permit
starting 4.4-4.11, whose subject is which events each producer records and with what field
coverage, not whether a path from producer to journal exists.

While resolving this, the following remain prohibited:

- `#[allow(dead_code)]`, `#[allow(unused)]`, or any other suppression of the unreachability report.
- Synthetic evidence: a startup marker, a dummy event, or any record describing work that did not
  happen. An event that exists to satisfy a linter is indistinguishable, once recorded, from an
  observation of real work.
- Widening the crate's public API to make an internal symbol appear reachable. `pub` would say the
  recorder is consumed from outside the crate, which is false; it would be the same suppression in
  a costlier form.
- Constructing a value only to discard it, `if false`, or an unreachable call.

Startup projection replay must be repair-if-needed rather than an unconditional full rebuild. A
projection that already agrees with the journal is rebuilt into itself, so an unconditional replay
buys nothing and costs a full journal scan on every launch -- and a scan whose only purpose is to
give the replay code a caller is exactly the kind of fake wiring this note forbids.

- [x] 3.16 Add domain tests that run without Tauri, SQLite, filesystem, network, or process dependencies.
- [x] 3.17 Add SQLite infrastructure tests for migration from the current schema, transaction rollback, indexes, cursor stability, replay, and retention.

## 4. Evidence Producer Integration and Coverage Gaps

- [x] 4.1 Define producer-owned semantic evidence output ports in Agent runtime, workspaces, operations/review, and sessions use cases that need them.
- [x] 4.2 Add bootstrap adapters that map producer semantic events to `execution_observability::api` inputs without exposing the evidence aggregate to producer domains.
- [x] 4.3 Add a bounded non-blocking publication queue and make evidence failure non-blocking to the owning Agent, Shell, log, review, or usage operation.
- [x] 4.4 Record safe run start/completion and observable tool/delegation lifecycle references from the existing canonical execution path.
- [x] 4.5 Record Session Shell opened/closed and structured command start/completion references when boundaries are observable.
- [x] 4.6 Record safe file-mutation observations after trusted workspace mutations or witnessed snapshot comparison; do not persist file content or full paths.
- [x] 4.7 Record review-level decision and automated verification outcome references. Hunk-level decisions and file Viewed resets are deferred to 13.2 and 13.5: neither has an authoritative store until 13.1 adds one, so a producer here would have to derive them from review-level state, and a derived observation recorded as an observed one is the confusion this journal exists to remove. The evidence contract for both is defined in design.md now so 13.2 and 13.5 publish against a settled shape rather than inventing one.
- [x] 4.8 Record usage-observed references that point to sessions-owned accounting observations without duplicating usage totals in the journal.
- [x] 4.9 Emit a bounded coverage-gap marker after queue overflow or persistence recovery, including counts and safe reason codes only.
- [x] 4.10 Add tests proving producer success is unchanged when the evidence recorder is unavailable or its queue is full.
- [x] 4.11 Add tests proving raw prompts, output, tool payloads, terminal text, code, diffs, secrets, environment values, and absolute paths are rejected or removed before evidence persistence.

## 5. Workspace Evidence Scope, Navigation, Summary, and Tab Lifecycle

- [x] 5.1 Add `WorkspaceEvidenceScopeProvider` scoped to the selected Session Workspace, using React Context and serializable state only.
- [x] 5.2 Implement atomic `navigate(target)` behavior that changes the active tab and evidence scope without resetting unrelated mounted panel state.
- [x] 5.3 Clear run/trace/span/operation/command/path/hunk scope when the selected session changes; preserve per-session panel view state where valid.
- [x] 5.4 Add active-filter chips and Clear Scope actions to panels that consume cross-panel scope.
- [x] 5.5 Add a `WorkspaceTabCapability` registry describing seat mode, live support, and retention policy instead of scattered tab-id conditionals.
- [x] 5.6 Pass `isVisible` to every mounted workspace panel.
- [x] 5.7 Suspend Logs/Traces live subscriptions, Report refreshes, and Files/Documents polling when their mounted panel is hidden.
- [x] 5.8 Detach the Shell xterm view when hidden while preserving the native Shell instance.
- [x] 5.9 Add one bounded `WorkspaceEvidenceSummary` query for tab badges and Basic Info health rather than mounting every panel query.
- [x] 5.10 Add accessible badge labels for Changes, Terminal History, Shell, Logs, Traces, and Report, and omit zero badges where the existing UI rule requires it.
- [x] 5.11 Debounce/coalesce identifier-only evidence notices and invalidate only affected query keys.
- [x] 5.12 Add frontend tests for navigation from log to span, span to command, command to file, finding to run, and summary row to owning tab.
- [x] 5.13 Add tests proving hidden mounted panels retain local selection/forms while live work is suspended.

## 6. Terminal History as Execution Records

- [x] 6.1 Add backend execution-record queries over command/tool/delegation/verification projections with stable cursors and coverage.
- [x] 6.2 Add a legacy message-history activity adapter without inserting historical `toolUse` rows into the native evidence journal.
- [x] 6.3 Mark legacy activity `inferred`, identify its message-history source, and expose coverage limitations.
- [x] 6.4 Refactor `terminal-tab.tsx` into a thin composition component plus record toolbar, virtualized list, row, detail drawer, and legacy-source notice components.
- [x] 6.5 Add filters for kind, status, seat, run, fidelity, and bounded text search.
- [x] 6.6 Render command runtime, redacted display, duration, status, exit code/signal, working-directory display, output availability, truncation, fidelity, and coverage without fabricating unavailable fields.
- [x] 6.7 Render tool, delegation, and verification records with their structured safe fields and source fidelity.
- [x] 6.8 Add cross-panel actions for Trace, Logs, Files/Changes, Report, and Shell when the corresponding target is available.
- [x] 6.9 Add page append that de-duplicates by stable record id and preserves loaded rows on failure.
- [x] 6.10 Virtualize the loaded record list and verify bounded mounted rows with maximum-page fixtures.
- [x] 6.11 Add empty, partial, indexing, unavailable, and no-filter-match states using synchronized locale resources.
- [x] 6.12 Add tests for native, proxied, inferred, opaque, running, failed, cancelled, incomplete, redacted, output-unavailable, and partial-coverage rows.

## 7. Retained Multi-Shell Lifecycle

- [x] 7.1 Add `SessionShellDescriptor`, state, runtime descriptor, output frame, attach snapshot with its `ShellAttachmentId`, replay gap, and typed Shell errors to the workspaces domain/application contracts.
- [x] 7.2 Replace the one-view lifecycle with a native `SessionShellRegistry` keyed by Shell id and indexed by session/seat.
- [x] 7.3 Implement `list`, `create`, `attach`, `detach`, `write`, `resize`, `rename`, and `close` use cases through application ports, with detach/write/resize carrying an attachment id so a stale view cannot detach or write into the attachment that replaced it.
- [x] 7.4 Retain UTF-8-safe sequence-numbered Shell frames up to 1 MiB per Shell and insert one gap marker when old content is evicted.
- [x] 7.5 Serialize concurrent create/attach requests so duplicate default Shells are not spawned for the same requested identity.
- [x] 7.6 Keep local PTY and remote SSH channel infrastructure behind workspaces ports; preserve independent remote channel lifecycle on pooled transports.
- [x] 7.7 Close inactive Shells after the configured idle window and close/join all Shell-owned workers during application shutdown.
- [x] 7.8 Publish state/output notices with sequence and timestamp; never publish unbounded replay in a Tauri event.
- [x] 7.9 Implement Tauri commands and adapters for all Shell registry operations, keeping existing create/kill commands as temporary delegating compatibility paths until frontend migration passes.
- [x] 7.10 Implement deterministic Web/mock multiple Shells, detach/attach replay, gap behavior, and explicit close.
- [x] 7.11 Refactor the Shell tab to render Shell tabs, Add, Rename, runtime/status metadata, and explicit Close confirmation.
- [x] 7.12 Change component cleanup and hidden-tab handling from close/kill to detach.
- [x] 7.13 Register the listener before attaching, buffer frames that arrive in the window, reattach using `nextSequence`, de-duplicate replay/live frames by shell id and sequence, and display a gap marker when content was evicted or dropped.
- [x] 7.14 Keep a foreground-process warning visible before explicit close when the runtime can report it; do not claim process state when opaque.
- [x] 7.15 Add local registry, remote channel, Web/mock, React lifecycle, and desktop E2E tests proving session/tab switches do not terminate Shells.
- [x] 7.16 Remove obsolete kill-on-unmount code only after the desktop Shell lifecycle tests pass.

## 8. Operations-Owned Log Query Index and Live Logs

- [x] 8.1 Define an `operations` application contract for indexed session-log queries, coverage, backfill status, live notices, and safe export preparation.
- [x] 8.2 Add additive SQLite migrations for the redacted log query index, correlation indexes, source-file checkpoints, gaps, and repair status.
- [x] 8.3 Publish an already-redacted log record notice from `platform::logging` after durable file append; do not expose unredacted input to the indexer.
- [x] 8.4 Implement idempotent index insertion keyed by stable record id and source file/offset witness.
- [x] 8.5 Implement stable keyset pagination and structured filters for level, text, session, run, trace, span, operation, agent, seat, and time.
- [x] 8.6 Add complete/indexing/partial/unavailable coverage with oldest/newest/indexed-through timestamps and dropped counts.
- [x] 8.7 Add a bounded asynchronous repair/backfill operation with stable operation id, cancellation, checkpoints, and unified diagnostics.
- [x] 8.8 Handle rotation, truncation, retention deletion, and configured log-directory changes without stale index rows claiming complete coverage.
- [x] 8.9 Keep unified redacted log files as the durable export/repair source and keep export behavior compatible.
- [x] 8.10 Move the implementation of session-log query commands from workspaces-owned file scanning to operations-owned APIs while preserving public command names/DTO compatibility during migration.
- [x] 8.11 Publish identifier-bounded post-commit live log notices through the Tauri adapter and deterministic Web/mock event stream.
- [x] 8.12 Upgrade Logs toolbar with Follow/Pause, correlation filters, coverage state, active-scope chips, and Jump to latest.
- [x] 8.13 Stop automatic viewport movement when Follow is paused or the user scrolls away from the newest edge.
- [x] 8.14 Insert a live row locally only when current filters can be evaluated safely; otherwise invalidate the first page.
- [x] 8.15 Keep loaded entries visible on refresh, live, and page-append errors and render inline retry/error status.
- [x] 8.16 Add tests for live insertion during pagination, cursor/filter mismatch, index repair, rotation, directory change, gap notice, retention, and export consistency.
- [x] 8.17 Add a maximum-fixture performance test proving the interactive query does not scan unbounded log files or block the shared registry on file I/O.

## 9. Trace Waterfall, Structured Span Kinds, and Evidence Links

- [x] 9.1 Add `ExecutionSpanKind` and safe evidence-count/link fields to observability timeline DTOs.
- [x] 9.2 Map pinned OpenTelemetry semantic conventions and documented `vanehub.*` attributes to span kind in Rust; remove React span-name substring classification.
- [x] 9.3 Derive bounded depth, start offset, completed duration, attempt, delegation, and critical-path metadata without inventing unavailable values.
- [x] 9.4 Add identifier-only run/span transition notices after committed timeline updates.
- [x] 9.5 Add deterministic Web/mock running-to-terminal trace transitions.
- [x] 9.6 Refactor the Traces tab into Run list, waterfall viewport, span row, legend/filter toolbar, and detail drawer components.
- [x] 9.7 Implement horizontal time scaling/scrolling and vertical virtualization for bounded span sets.
- [x] 9.8 Add keyboard selection, accessible status/fidelity labels, focus visibility, and narrow-width drawer behavior.
- [x] 9.9 Add detail sections for Overview, safe Attributes, Events, Logs, Commands, Files, Findings, Usage, and Error.
- [x] 9.10 Query linked evidence from its owning service using the shared scope rather than embedding raw log/file data in the trace DTO.
- [x] 9.11 Add live refresh only while Traces is visible and coalesce rapid notices.
- [x] 9.12 Add critical-path, retry, delegation, failed-span, fidelity, and coverage filters.
- [x] 9.13 Add optional two-run comparison for status, duration, usage quality, tool counts, failures, and changes without comparing raw content.
- [x] 9.14 Add unit, accessibility, maximum-span performance, and desktop rendering tests for both visual styles.

## 10. Backend Session-Run Report

- [x] 10.1 Add report query/result DTOs, section coverage, overview, usage, latency, Agent, tool, command, change, verification, failure, and evidence-link models under the sessions application boundary.
- [x] 10.2 Add narrow sessions-owned ports for execution evidence, observability timing, operations log summaries, workspace/review summaries, and existing session usage summaries.
- [x] 10.3 Wire report ports through bootstrap adapters to published context APIs without direct repository or infrastructure imports.
- [x] 10.4 Implement bounded report scope validation for session, run ids, seat ids, date range, and group-by dimensions.
- [x] 10.5 Preserve reported, reported-derived, and estimated usage separately and preserve internal-purpose versus user-response consumption.
- [x] 10.6 Return unknown/partial coverage rather than substituting zero for missing evidence.
- [x] 10.7 Omit monetary cost or mark it unavailable unless an explicitly versioned provider-pricing observation exists; do not introduce a pricing catalog in this change.
- [x] 10.8 Add Tauri and Web/mock report methods and contract tests. This registers the session-run report command and activates the report native methods added in 2.4: the production Tauri binding stops returning unavailable, and the 2.6 conformance suite runs against those native report cases.
- [x] 10.9 Replace React message aggregation in Report with the backend service query; retain a legacy comparison test until parity behavior is understood.
- [x] 10.10 Build Report sections for Overview, Usage, Latency, Agents, Tools, Changes, Tests, Failures, and Evidence.
- [x] 10.11 Add scope controls for run, seat, time, and group-by, preserving previous report content while refresh is in flight.
- [x] 10.12 Add evidence links from every report section to the corresponding workspace tab and filter.
- [x] 10.13 Add JSON export of the bounded report through a service-backed native destination flow and a clearly simulated/unavailable Web path, if the existing export primitive can be reused without adding arbitrary frontend file writes.
- [x] 10.14 Add application tests for complete, partial, unavailable, no-usage, child-Agent, retry, test-failure, changed-file, and no-evidence reports.
- [x] 10.15 Add UI tests proving Report results do not change when message pagination or mounted chat-message count changes.

## 11. Provider-Neutral Local and SSH Workspace Inspection

- [x] 11.1 Add `WorkspaceTarget`, `WorkspaceInspectionCapabilities`, typed provider errors, and a `WorkspaceInspectionProvider` application port under workspaces.
- [x] 11.2 Adapt existing confined local file/document/search/Git implementations behind `LocalWorkspaceInspectionProvider` without weakening path, symlink, size, locale, or diff bounds.
- [x] 11.3 Resolve provider selection from the registered Session binding; never accept a frontend-supplied absolute root as authority.
- [x] 11.4 Publish only the required remote exec/channel contract through `ssh_connections::api`; do not import its pool or infrastructure modules.
- [x] 11.5 Implement a versioned static SSH helper protocol that sends bounded JSON over stdin and receives bounded JSON output.
- [x] 11.6 Probe POSIX remote host, current profile revision/host trust, Python 3 helper support, Git, and ripgrep capabilities and return typed per-feature availability.
- [x] 11.7 Implement remote realpath confinement, symlink-escape rejection, bounded directory listing, bounded text preview, and deterministic sorting inside the helper.
- [x] 11.8 Implement bounded remote path/content search through `rg --json` when available and return typed unavailability otherwise.
- [x] 11.9 Implement remote Git status/diff using argument-array subprocess calls and the existing normalized structured DTOs; preserve index/worktree distinction and locale-independent classification.
- [x] 11.10 Make remote inspection retries idempotent and revalidate profile revision and host trust; never replay a mutation or Shell command.
- [x] 11.11 Add deterministic Web/mock local/remote capability fixtures and read-only inspection data.
- [x] 11.12 Add provider contract tests that run the same list/read/search/status/diff/path-escape cases against local, remote-helper fixture, and Web/mock implementations where applicable.
- [x] 11.13 Add typed unavailable UI states that preserve Shell access and explain missing helper/Git/ripgrep prerequisites.
- [x] 11.14 Add an optional isolated SSH integration test gated by explicit environment configuration; keep normal CI independent of external SSH.

## 12. Files and Documents Workflow Upgrade

- [x] 12.1 Add stable per-directory continuation cursors and query-bound validation to file listing DTOs and providers.
- [x] 12.2 Add normalized workspace invalidation notices from local watch, remote polling, and execution-evidence file mutations.
- [x] 12.3 Invalidate only affected tree, preview, document, search, Git, diff, and review query keys; retain current selection when still valid.
- [x] 12.4 Add Quick Open path search with keyboard navigation, cancellation, stable ordering, provider coverage, and bounded pages.
- [x] 12.5 Add content search with line, column, bounded redacted snippet, provider coverage, cancellation, and result-to-preview navigation.
- [x] 12.6 Add Files toolbar actions for Quick Open, Content Search, Refresh, Copy Relative Path, Reveal/Open externally when supported, and Open Shell at directory when supported.
- [x] 12.7 Upgrade text preview with line numbers, syntax highlighting through existing libraries, in-preview find, line navigation, encoding/newline metadata, and evidence actions.
- [x] 12.8 Keep the previous file preview visible while a new selection or refresh loads; show stale/refreshing status and preserve another selection after failure.
- [x] 12.9 Add Recent Documents, document path search, source/preview modes, and heading outline derived from bounded content.
- [x] 12.10 Reuse existing safe Markdown, link, image, code, math, and Mermaid handling; do not introduce direct HTML execution.
- [x] 12.11 Add document/file links to related execution runs, commands, Changes, and review findings through the shared evidence scope.
- [x] 12.12 Add per-provider partial/truncated/unavailable states instead of one ambiguous global partial marker.
- [x] 12.13 Preserve read-only semantics and omit create/edit/rename/delete/save controls from this change.
- [x] 12.14 Split Files/Documents production components and hooks to remain within the line-size rule. Every Files/Documents module built in 12.1-12.13 was already inside it, so the splits this task anticipated had happened as the features were written. What had not: the tab host reached 282 lines from navigation callbacks added a round at a time, and the pre-existing logs hook sat at 290. A headroom guard at 280 now names the worst offender, which is what surfaced both.
- [x] 12.15 Add maximum-directory, maximum-search, large-preview, invalidation, stale-content, remote-unavailable, keyboard, and visual-style tests. Three suites split by what fails, not by which component: bounds (a full page is not a cut one), freshness (the workspace changed under the reader), and presentation (the keyboard contract both dialogs claim to share, and colour taken from tokens). The keyboard suite surfaced one accessibility gap that belongs to 14.9 and is recorded there rather than left in a comment.

## 13. Review Hunk State, Viewed Progress, Patch Copy, and Evidence

- [x] 13.1 Add additive SQLite migrations for review hunk decisions and review file Viewed state keyed to current snapshot witnesses. Migration 83 adds `review_hunk_decisions` and `review_file_states` in the sessions context beside the review aggregate migration 75 established, each row carrying the snapshot fingerprint it was recorded against. Two deviations from design.md's sketch, both stated in the schema: timestamps are RFC3339 text because that is what `ReviewClockPort` returns and what every other review table stores, and both tables cascade from `review_sessions` so retention does not need a second sweep for rows nothing can reach. Numbering is now colliding on every version from 81 up — main has landed 81 through 86 under other names — so 83 carries the same repair 81 does, and the note above them records which versions this branch renumbers past on merge.
- [x] 13.2 Extend the review aggregate/application service to persist review-level and hunk-level decisions independently, and publish a hunk decision evidence reference after the decision commits. This completes the half of 4.7 that had no authoritative store to observe. Independence is structural, not conventional: a hunk decision goes through its own `ReviewDecisionRepository` port because `ReviewRepository::save` rewrites the whole aggregate, so a hunk write that shared it would rewrite the review's decision too. The service records the review's own snapshot fingerprint rather than a caller-supplied one, so nothing stored is ever a claim about a diff that was not under review — 13.3 adds the caller's expectation and the refusal when they disagree. The command that reaches this is 13.4's, so the entry points carry `expect(dead_code)` rather than `allow`: wiring the command deletes those lines rather than leaving them.
- [x] 13.3 Reject stale hunk decisions when review, file, or hunk witnesses no longer match and return `stale_witness` without mutation. Checked cheapest first — snapshot and file are answered from the review already in memory, so a stale snapshot never costs a workspace diff read — and all three return before the upsert, so a refusal never leaves a decision recorded against a diff the service just said it cannot vouch for. The hunk witness reaches the workspace through a new `ReviewHunkWitnessPort` over the same bounded `load_review_file` the Review Center renders from; a second way of computing fingerprints would eventually disagree and make every decision look stale. A read that cannot be answered is a refusal, not a pass. One reason code crosses as the whole message rather than inside a sentence; which witness failed is kept for 13.10's message and the log.
- [x] 13.4 Add `setCodeReviewHunkDecision` Tauri command, Tauri/Web adapters, DTO schemas, and contract tests. The service interface, the input/receipt types, and the Web fixture already existed; what was missing was the native command and the Tauri adapter, which rejected with `review_hunk_decision_unavailable` rather than falling back to the review-level mutation. Wiring it retired 13.2's write-path `expect(dead_code)` lines, which is what they were for. Two follow-on corrections: the Review Center branched on a reason code nothing can emit any more, so it now names the one refusal a reviewer can act on (`stale_witness`), and the message telling them the desktop runtime does not store hunk decisions was deleted in all five locales rather than left to say something false.
- [x] 13.5 Add `setCodeReviewFileViewed` and reset a file to unviewed when its snapshot fingerprint changes, publishing a file Viewed evidence reference after the state commits. This completes the other half of 4.7. **Deviation from design.md, deliberate:** the mark is witnessed to the *file's* own fingerprint, not the review snapshot. A review snapshot fingerprint covers every changed file, so witnessing Viewed to it would clear all twelve marks because an agent wrote to one — "8 files · 4 unviewed" would reset on unrelated work and stop being a number anybody could act on. Migration 84 adds the `file_witness` column for that; the snapshot column stays and stays truthful, recording which snapshot the reviewer was in when they marked it. Nothing sweeps on reconcile: a mark stops applying because its stored witness stops matching the file. Only reading is published — unmarking is a reviewer taking a claim back, and "somebody stopped having read this" is not an observation. The witness is derived from all five file fields, so a file that went from modified to deleted is not the file that was read.
- [x] 13.6 Add review summary fields for viewed/current file counts and unresolved comment/finding counts. Derived on every read rather than stored: a stored count is a second answer to a question the rows already answer, and the two disagree the first time anything writes without updating both. The viewed count walks the review's *current* files and asks whether each has a mark made against the version that is there now, which is where 13.5's reset actually happens — nothing sweeps, and it is therefore right for changes nobody published. `ReviewSessionDto` is now built only from a `ReviewView`; the conversion from a bare `ReviewSession` was removed rather than left to invent a viewed count, because the only value available to invent is zero and zero reads as "you have read none of these" rather than "this path could not find out". Unviewed is not shipped: it is the subtraction, and a fifth number can disagree with the two it came from.
- [x] 13.7 Add `getCodeReviewPatch` using the existing native structured diff/patch renderer and requiring the current snapshot witness. Session-scoped rather than review-scoped, matching `revertCodeReviewChange` beside it: the workspaces context knows sessions and snapshots, and the review id lives in another context. The witness check, path resolution, and hunk-selection ambiguity check now come from one helper shared with the revert, because they answer the same three questions and two copies would drift into two answers. **0.8's gate immediately earned itself:** the existing renderer wrote `--- a/path` for files with no old side and `+++ b/path` for files with no new side, which reads correctly and which `git apply` refuses — invisible until now because its only caller applied patches in reverse against files that already existed. Fixed by deriving the two `/dev/null` sides from the hunks rather than from `change_type`, whose `old_hash` is always absent here anyway. The patch fingerprint and the size bounds arrive with 13.8.
- [x] 13.8 Bound patch output, return its fingerprint, and reject binary, oversized, ambiguous, or stale requests. The patch is refused over its bound rather than truncated: a patch cut short looks exactly like one that applies until somebody runs it somewhere it matters. The fingerprint is over the patch bytes and sits beside the snapshot rather than replacing it — the snapshot says which diff it came from, the fingerprint says whether two renders are the same copy, and a patch held on a clipboard needs both questions answered. Four stable codes rather than one shared "unavailable", because each is a different sentence: `stale_witness` (the same code the hunk-decision path uses, for the same fact), `review_hunk_unavailable`, `patch_unavailable_binary`, `patch_too_large`. A `WorkspaceApplicationError::Conflict(&'static str)` carries them as the whole message, matching 13.3 — a code inside a sentence makes every caller parse prose to find it.
- [x] 13.9 Enable Copy Standard Patch and keep Copy Displayed Lines as a separate action. Kept separate rather than folded together: a reviewer quoting three lines into a message wants exactly those lines with no headers around them, and one action cannot be both. The standard patch is fetched from the side that can see the repository and witnessed to the snapshot on screen — assembling it in React from the rendered hunks would produce something that reads like a patch and that Git refuses. Each of 13.8's four refusal codes gets its own sentence, because "could not copy" is true of all four and useful for none. The `copyStandardPatchPending` message was deleted from all five locales rather than left saying a shipped feature is unavailable. Two test traps worth knowing: `renderWithAppProviders` calls `userEvent.setup()`, which installs its own `navigator.clipboard` and silently replaces a mock placed in `beforeEach`; and the copy buttons render as soon as the review loads, so waiting on them lets a click land while the diff is still null and copy an empty string.
- [x] 13.10 Add Review header progress, Mark Viewed/Unviewed, independent hunk decision controls, and stale-state presentation. Every review read now carries the recorded hunk decisions and a `viewed` flag per file, which is what retired the last read-path `expect(dead_code)`: without them the panel could only show what it had set in the current session, so a reload would render every decision as undecided while the store held them. Decisions are matched by hunk fingerprint rather than by snapshot — the same reasoning as 13.5's per-file witness, since a review snapshot moves whenever any file does. The hunk control has three states, not a checkbox: a hunk somebody read and asked for changes on is a different thing from one nobody has reached, and pressing the control that already holds a decision clears it, because a reviewer who accepted by mistake has nowhere else to go. Zero counts are not rendered — a review with nothing outstanding should read that way rather than as a row of zeroes. Every write re-reads the review instead of patching locally, so the header agrees with what was recorded rather than with the click. Split into `review-progress.tsx` and `use-review-marks.ts` to keep the tab host inside its line budget.
- [x] 13.11 Link automated findings to run/operation/span evidence and navigate through the shared scope. A finding used to render as one line of text — a machine's assertion with nothing beside it, which a reader either believes or ignores. It now carries a link to the run that produced it and, when anchored, to the code it is about. The operation id is the only correlation a finding holds and it is enough: the records tab resolves the run, the trace, and the span from it, and carrying those here would be three identifiers this side has to keep agreeing with a store it cannot read. Two shapes worth noting: the code link selects the file *inside* the Review Center rather than navigating, because the Review Center is the Changes tab and going there would be a no-op that reads as a broken link; and the run link comes in as a callback prop rather than from `useWorkspaceEvidenceScope`, which throws without a provider and would make every standalone panel test unrenderable — the same trap 12.6 hit.
- [x] 13.12 Add tests proving review acceptance and hunk acceptance remain independent, Viewed resets on snapshot change, and no decision action stages or rewrites Git content. The first two are checked as properties over a whole review lifecycle rather than as screens — including the half the wording does not name, that a mark survives an edit to a *different* file, which is the behaviour 13.5's per-file witness exists for. The third is a source scan on both sides: a behavioural test can only fail for the mutation somebody thought to write a case for, and here the revert lives one import away from all three decision surfaces, adjacent to them in the same header. Both scans carry a positive control against the one path that legitimately mutates, so a typo cannot leave a guard that passes by matching nothing.
- [x] 13.13 Add native fixture tests proving generated file/hunk patches pass `git apply --check` for current snapshots and fail closed for stale snapshots. End-to-end through `render_review_patch` — a real repository, a real session row, a real snapshot — rather than through the renderer alone, so the witness, the path resolution, and the hunk selection are all in the path being checked. Two findings on the way: a review patch has to be checked against the **index**, not the working tree, because it describes committed content becoming working-tree content and the working tree already holds the change — checking there asks whether it applies on top of itself, and the refusal names the old lines that are gone. And the first fixture produced one hunk rather than two, because three lines of context on each side fold any two nearby edits together; a single-hunk selection cannot be tested against a diff that only has one.

## 14. Information Panel, Visual Integration, Accessibility, and i18n

- [x] 14.1 Add the evidence-aware runtime/workspace/Shell/change/verification/diagnostic/usage summary to the existing Basic Info pane through `WorkspaceEvidenceSummary`. Every number comes from the one summary read the workspace already makes — the same query key the tab badges use — so Basic Info cannot quietly disagree with the tab bar. Two facts are deliberately derived rather than fetched: "dirty" from the change count in the row below it, and the workspace provider from the capabilities query the Files/Documents/Changes tabs already share. A failed read says so instead of rendering zeroes, because "nothing has happened" and "nothing could be read" are identical as numbers and lead to opposite conclusions; the same reasoning keeps an absent token total from rendering as "0 reported". The runtime row shows the moment a run started rather than an elapsed time computed here, which would tick against a clock the backend does not share and keep ticking for a run that ended while the panel was hidden.
- [x] 14.2 Make every summary row navigate to its owning workspace tab and preserve the current Session. Reuses the existing `requestedTab` + nonce mechanism the slash commands already use, so the session is preserved by construction and a reader who clicks Changes, walks back to Chat, and clicks Changes again gets it both times. Loop inspection still wins: it is showing another session's transcript, and a row that moved the tab under it would leave the reader looking at this session's workspace beside that one's messages. Usage stays in this panel rather than going to a workspace tab — it is a pane here, and routing it through the same callback would make one destination mean two kinds of place. Two accessibility fixes fell out: the row list is a `ul`/`li`/`button` rather than a `dl`, because a `button` is not a legal child of `dl`; and each row is named explicitly, since the gap between label and value is flex spacing rather than a text node and the computed name would announce "Changes8 files".
- [x] 14.3 Preserve the existing Basic Info, Token Usage, Skill, optional Members, IM, and Code Index tab set and state semantics. Held as a shape rather than as contents: which tabs exist for which session, that the two optional ones appear only when they should, that every pane stays hidden rather than unmounted across a switch, and that a failed evidence summary costs the reader the summary and nothing else. The test-writing trap worth recording: the Skill pane renders its own Effective/Global/Project tablist inside the panel, so an unscoped `getAllByRole("tab")` returns seven tabs from two unrelated controls and any assertion about "the tab set" is quietly about both.
- [x] 14.4 Keep inactive information-panel panes mounted but disable queries/subscriptions while inactive unless an in-flight mutation must finish. All five panes take an `active` flag and gate their reads on it; they stay mounted, so a reader who typed something, checked another tab, and came back finds it there, and React Query keeps the last answer so returning shows it immediately while the refetch runs. The IM pane's connector lifecycle *subscription* is the expensive half — a hidden pane held a live channel for updates nobody could see — so `active` is a dependency of that effect rather than a guard inside it, which also makes hiding run the cleanup that cancels a pending pairing. The mutation caveat needed no machinery: React Query runs a mutation to completion regardless of `enabled`, so a write in flight when the reader switches away still finishes and still invalidates.
- [x] 14.5 Add compact tab/badge, record list, Shell tabs, Logs toolbar, waterfall, Report, Files, Documents, Review, and Basic Info styles using shared semantic tokens only. All ten surfaces were already clean — the styles were built with tokens as they were written — so the work was making that hold rather than repairing it. The guard names each surface individually so a rename fails here rather than going unscanned, and then sweeps both directories so a helper module with a hard-coded colour cannot slip past the named list. 12.15's Files/Documents guard now reads the same two patterns from the same module: two copies would drift, and the copy that drifts is the one with fewer readers. One module is excluded by name — the module that *defines* the patterns, whose anti-vacuity samples are hard-coded colours on purpose; scanning it would mean deleting the sample to satisfy the rule the sample exists to protect.
- [x] 14.6 Verify no hover, loading, active, failure, or badge state changes control dimensions or shifts adjacent content. Verified and then held. jsdom has no layout engine, so nothing here measures a box — what it reads is the classes that decide the box, which is where the mistake actually lives: `hover:px-3` is a padding change written down, and it is invisible in review precisely because it looks like styling. Two purely syntactic patterns (a state variant carrying a dimension utility, and a condition adding spacing on its own) plus two rendered checks: the shared `WorkspaceState` gives loading, empty, unavailable, and error one container size and one icon size, and a badge is the same size in every tone and as the unknown marker. Only the sizing classes are compared — loading legitimately draws a different glyph and spins it, and comparing whole class lists would fail for the two things this rule permits while saying nothing about the one it forbids.
- [x] 14.7 Add synchronized translations for every new label, tooltip, filter, status, coverage state, error, empty state, confirmation, drawer tab, and accessibility name in every registered locale. Every string was added to all five locales as it was written, so the parity suite never went red and this task found nothing to backfill: 4,197 keys in each of en, zh-CN, zh-TW, ja, ko, zero missing. Two things were deleted rather than translated, in all five — `copyStandardPatchPending` and `hunkDecisionUnavailable` both told a reader a shipped feature was unavailable. The CJK and Korean plural pairs carry the same sentence in `_one` and `_other`; those languages do not inflect for number, and the pair exists because i18next requires it rather than because the two differ.
- [x] 14.8 Run and fix i18n resource parity and visible-text guardrails without broad allowlisting. Both pass, and the visible-text guardrail gained the fourteen surfaces this change built. But that guardrail is a list of strings that were once hard-coded, so it can only fail for copy somebody already wrote down — the next hard-coded sentence, the one nobody has thought of, passes it. So a second guard reads the JSX and asks the opposite question: is there prose here that did not come from `t()`. It found three, all product names, which are now exempt individually rather than by pattern — a pattern would grow to cover whatever failed next, and "this is a product name" versus "nobody translated this yet" is exactly the judgement a pattern cannot make. A further case checks the exemption list still names strings that are actually rendered, so an entry cannot outlive the string it was written for.
- [x] 14.9 Add keyboard navigation and focus management for execution rows, Shell tabs, log follow controls, waterfall rows, detail drawers, quick open, document outline, and review actions. Both search dialogs are now comboboxes over listboxes — the only shape that makes "focus stays in the input while the highlight moves" announceable, since without `aria-activedescendant` the DOM focus never moves and a screen reader has nothing to read on the down arrow. The `role="option"` moved from a button onto the list item, because a button inside an `li` puts a listitem between the listbox and its options and its interactive descendant is what breaks the pairing; the empty-state message moved out of the listbox, where it announced itself as one more thing to choose from. Two further gaps found rather than assumed: the record detail drawer took focus on open and never gave it back, so closing one dropped a keyboard reader onto the document body; and the session context menu could only be dismissed by clicking the scrim, so a reader who opened it with the keyboard had no way out at all. The guard that found the second one keeps the debt honest — a scrim is exempt from needing a tab stop, and in exchange every module with one must answer Escape.
- [x] 14.10 Verify icon-only controls have localized accessible names/tooltips and status is not communicated by color alone. The names were already there and are now checked by rendering rather than by reading: Testing Library computes an accessible name the way a browser does, from label then title then text then `sr-only`, and a source scan has to reimplement that and gets it wrong in the cases that matter. The colour half found a real one. The unified diff has always prefixed each line with `+`, `-`, or a space; the split view rendered bare content, so a deletion on the left and an addition on the right were the same string in two differently tinted boxes — the readers who most needed the marker were the only ones without it. The sign now comes from one shared helper so the two views cannot drift again. Worth recording is what the colour half could *not* be: a source scan flagged two modules and one was innocent, because the line that gets this right — `entry.level === "error" && "text-destructive"` on the span holding the word `error` — is written identically to the line that got it wrong. What separates them is whether the coloured element has text of its own, which is a fact about the rendered tree and not about the source, so this rule is held by rendering an addition beside a deletion and comparing what they say.
- [x] 14.11 Perform visual tests in `futuristic` and `minimal` styles at desktop and narrow widths, including partial/error/live states and long localized labels. Four variants plus a Korean narrow case, in a real browser, because everything up to here ran in jsdom and jsdom has no layout engine — it can read the classes that decide a box but never the box. The states are not staged: the Web fixtures already carry a running record, a failed verification, and per-section partial coverage, so these read what the app renders rather than a scenario assembled to look interesting. The rule about width is not "nothing is wide" — a diff row carries a minimum so code stays legible instead of reflowing — but "whatever is wide can be scrolled to", read off the computed `overflow-x` rather than off the class, since the class is what somebody deletes and the computed style is what the reader gets. Proved non-vacuous by removing the review center's scroll container: the two narrow variants failed and both desktop ones stayed green, which is exactly the split the rule claims to draw. And the browser found what jsdom could not have: the Changes tab renders the *review center's* diff, not the one 14.10 repaired, and both of its views separated an addition from a deletion by background tint alone — on lines that are buttons, so the accessible name a reviewer hears was the line number and the text with nothing to say which side of the change it was on. All four views now take the sign from one helper, and "copy displayed lines" copies what is displayed, signs included.

## 15. Migration, Backfill, Retention, and Cleanup

- [x] 15.1 Make every new migration additive, idempotent, centrally ordered, and covered by a current-database upgrade fixture. Four migrations — 81 evidence journal, 82 log query index, 83 review decisions, 84 file-viewed witness — all additive and all ordered in `EXPECTED_MIGRATIONS`, which is what the startup density check reads. The gap this found is in what "idempotent" has to mean here. Every statement in 82 is already `IF NOT EXISTS`, so calling it twice was safe; what it lacked was a repair, and being safe to call twice does not help something the version gate calls zero times. All four numbers are taken on `main` by unrelated migrations, so a shared application database migrated by another worktree arrives with the history complete and the schema absent — 81, 83, and 84 each carried a repair for exactly that, and 82 was the one that did not. The upgrade fixture is the four-way version collision rather than a fresh install: migrate, seed a session, drop all nine tables while leaving `schema_migrations` intact, migrate again, and require every table back with the session still there. Proved non-vacuous by removing 82's repair, which makes it name the four `unified_log_*` tables rather than fail on a count. A second case runs `migrate` twice over a complete database and checks a recorded checkpoint survives: a repair that recreated its tables would reset every checkpoint on the next startup, and the only symptom would be a log index reindexing from zero for no stated reason.
- [x] 15.2 Start evidence capture only for new observable events; keep legacy message activity separate and labeled inferred. The projection half was already built in 6.2 through 6.4 and has its own suite; what was missing is the rule that keeps it a projection. Migration 81 creates the journal on databases holding years of chat history, and the tempting next step is to walk `messages` for `toolUse` blocks so an old session has something to show — which is the one thing this capability cannot do, because a `toolUse` block is what an assistant said it was doing and the journal's entire value is that everything in it was watched happening. Once the two are filed together nothing downstream can tell them apart: not the fidelity column, which a backfill would have to invent, and not the reader, who has no reason to suspect the question is worth asking. Two rules now hold it. A source guard fails any file that both appends to the journal and reads the chat corpus — the pair, not either half, since files doing one or the other are expected — and it names the two files that write the journal, so a rename fails it rather than leaving it scanning nothing. A migration test upgrades a database that already holds a `toolUse` message and requires the journal to come out empty and the message to still be there. Empty is the correct answer here and it is the one that looks like a bug, which is exactly why it needed writing down. Proved non-vacuous by planting a real backfill statement, which the guard names with its file and its repair.
- [x] 15.3 Start log-index repair asynchronously after startup/configuration without blocking the main thread, React, or unrelated Tauri commands. Two of the three were already held: the job is spawned rather than run, assembly performs no repair, and the runtime starts it exactly once after the window exists. The third had no rule. `repair()` is synchronous SQLite and filesystem work, and calling it from an `async fn` compiles, starts instantly, and parks one of the runtime's few worker threads for the length of the scan — every unrelated Tauri command scheduled onto that worker waits behind it. What makes that one worth a guard rather than a review comment is that it has no symptom anybody would recognise: startup is still fast, the repair still finishes, and the user sees a handful of commands that are slow once, shortly after launch, on a machine with a lot of retained logs. It reads as the machine. So the shape is asserted — the await is on a `spawn_blocking` handle and the bare call is absent — and proved non-vacuous with the mistake that would actually be made, `spawn` in place of `spawn_blocking`, which compiles and which the guard names.
- [x] 15.4 Persist repair checkpoints and resume after restart without duplicating indexed rows. The store already committed rows, gaps, and checkpoint in one transaction and already made a replayed batch a no-op; what was untested is the restart itself, and that is the case where the two halves come apart. The resume point is read from the database on every pass rather than carried in the repair's own state, and that distinction is invisible while the process stays up — an in-memory offset and a persisted one behave identically until something restarts. What a lost checkpoint costs is not correctness, since the unique constraint on `record_id` absorbs the duplicates, but a full re-read of every retained log file on every launch: silent, unbounded, and growing with how long the user has kept the app. Three cases now: a restart resumes from the persisted offset and the next batch appends rather than re-inserting; a batch committed before the restart inserts nothing after it; and a recreated file under a new generation does not inherit the old one's offset, which is what keying checkpoints by generation rather than by path is for — a rotated file reuses the path, and resuming at an offset written for the old one would skip the beginning of the new one with nothing to say so. The restart is the same directory with everything in-process dropped, and pointing it at a different directory turns the first two red, which is the proof that they read disk rather than memory.
- [x] 15.5 Coordinate evidence/log-index retention with source trace/log retention and expose partial coverage when source rows are no longer recoverable. The evidence half was already coordinated and already honest: expiring events marks the session `retention_trimmed`, records the oldest that survives, and the coverage read degrades to partial with `retention_expired`. The log-index half was coordinated and silent. `expire_sources` deletes rows whose file is no longer in the retained inventory — which is the coordination, and is correct — but it recorded nothing, so a query after a log rotation returned fewer rows and called itself complete. That is the one deletion in the repair that cannot be undone: every other row it removes can be read again from the file it came from, and a row whose file is gone has nothing left to re-read. Silence there tells a reader the work never happened rather than that the record expired, which is the exact distinction this console exists to keep. Expiry now writes a `log_source_expired` gap per source in the same transaction as the delete — two statements would leave a window where a crash loses the rows and keeps no record of having lost them, an index that is short and says it is whole. The batch is read before it is deleted rather than counted up front, because counting the whole expired set would attribute rows to this batch that a later batch deletes and the gaps would sum to more than was ever indexed. A third case holds the part that was already right but only implicitly: `clear_gaps_through` is scoped to the sources a pass covered and an expired source is never among them, so widening that clear — which would look like tidying up — is what would silently restore the "complete".
- [x] 15.6 Add database capacity/maintenance tests for maximum bounded fixtures and delete expired projections without per-event full scans. The existing retention test uses four rows against a bound of five hundred, so the bound it is named for had never once been exercised — a pass that quietly deleted everything in one statement would satisfy it, and on a machine with a year of history that is the whole database locked at startup. Both sides now have a fixture built at the real bound, taken from the constant rather than from a literal beside it, since a fixture holding its own copy of the number stops testing the boundary the moment the real one changes and goes on passing while it does. Evidence retention stops at `RETENTION_BATCH` and drains on the next pass; log expiry does the same at `REPAIR_PRUNE_ROWS`, which also exercises something 15.5 introduced — naming every deleted row means a full batch is five hundred bound parameters in one statement, and SQLite's variable limit is a ceiling nobody thinks to look for whose failure lands on exactly the databases large enough to need the pass. The scan half is read off `EXPLAIN QUERY PLAN`: the outer delete seeks the expired window and the correlated survivor check reaches the journal through its session index, with no `SCAN` anywhere. Worth recording is that the first version of that assertion asked for `SCAN execution_evidence_events` and passed while checking nothing — SQLite reports the subquery under its alias, so the string never appears under any plan. The anti-vacuity assertion beside it is what caught that, which is the argument for writing one every time.
- [x] 15.7 Remove obsolete offset-only log query internals after indexed query parity and migration tests pass. The removal itself was already done and already guarded: `list_session_logs` reads the index with no fallback, no query command reaches the file scanner, and the retired file-scanning command is not registered. What none of those cover is the pager coming back by a different route, so the rule is now on the shape rather than on the retired name. An offset names a position in a result set, and this result set grows underneath the reader continuously — every row that arrives renumbers every position after it, so paging by offset skips exactly the rows that appeared while the reader was reading, and skips them silently, leaving a page short by however many arrived with nothing to say so. Matched on the SQL keyword only, with a case proving it separates that from `source_offset` and `next_offset`, which are byte positions in a log file and are all over the same three modules.
- [x] 15.8 Remove obsolete Shell kill-on-unmount and string capability compatibility paths after frontend and desktop tests pass. Both are gone and the first is thoroughly held — the retained registry is the only lifecycle owner, no one-view command is registered, and no production file constructs the retired service. The capability half had nothing. The string union it replaced let the UI offer resize to a simulated shell and reconnect to a PTY, because a string carries no constraints; the union that replaced it puts each capability inside the variant that has it. But the field the frontend narrows on exists only because of one serde attribute: remove `tag = "kind"` and serde falls back to external tagging, `kind` reads undefined on every descriptor, and every narrow falls through to its last branch. Nothing else catches that — TypeScript will not, because the frontend type is declared rather than derived, so it goes on claiming a field the wire no longer carries and the failure surfaces as a shell quietly reporting the wrong capabilities. The guard asserts the attribute sits on that declaration specifically, and that all four frontend variants still carry their own `kind`; removing the attribute makes it fail with the attribute it found instead.
- [x] 15.9 Remove React message-derived Report computation after backend report parity tests pass; retain only explicitly labeled legacy activity projection. The Report tab reads the backend service and imports nothing from the old aggregation, and the legacy activity projection is retained as its own labeled list. `aggregateSessionReport` itself stays, deliberately: `report-legacy-parity.test.ts` holds the divergences between the two implementations as tests, and those divergences are the reason for the replacement — deleting the old code would delete the record of what its numbers meant, which is what invites somebody to "fix" the backend by making it agree with them again. What was missing is that its retirement rested on a sentence at the top of the file, and a sentence does not fail a build. The mistake is plausible in a specific way: the function is exported, its name is exactly what somebody adding a session summary would search for, and it returns numbers that look right for whatever history happens to be mounted — while being a function of scrolling, since paging older messages in changes the totals. A guard now requires the only file mentioning it to be the one defining it, and adding a reference from the Report tab makes it name that file.
- [x] 15.10 Update `src-tauri/ARCHITECTURE.md` with evidence-journal, operations log-index, report composition, and workspace-provider decisions. Four ADRs, 006 through 009, each stating the decision and the failure it rules out rather than restating the module layout — the inventories above already say where the code is, and a decision record that repeats them ages into a second, wrong copy. ADR-006 is the journal's one rule, that it holds observations and never claims, with backfill named as the specific thing that would destroy the distinction. ADR-007 is the index as a rebuildable projection: why the repair runs off the async pool, why checkpoints key on generation rather than path, why paging is keyset, and why expiry is the one deletion that has to leave a mark. ADR-008 is Report composed from evidence, including why the retired React aggregation is deliberately kept uncalled. ADR-009 is one provider per session reporting capability by capability, plus the single shell lifecycle owner and the tagged runtime descriptor. The migration inventory was left alone on purpose: it is a refactor plan with a "Target owner" column, covering the versions that refactor moved, not a running list of every migration — appending 81 through 84 to it would be answering a different question than the table asks.
- [x] 15.11 Update developer documentation for evidence fidelity/coverage, Shell attach/detach, remote helper prerequisites, log indexing, and cross-panel navigation. One new chapter rather than five edits scattered across existing ones, in both `src` and `zh-CN` since the guide is a full mirror and a chapter in one language only is a chapter half the readers cannot find. The five topics belong together: they are one capability seen from five angles, and a developer touching any of these tabs needs all five before the first of them makes sense. What the chapter carries is what the code cannot say — fidelity is about a record and coverage is about an answer, and the distinction that matters most is between "this is everything" and "this is as much as was looked at"; a shell outlives its view and unmount must not kill it; a remote provider reports capability by capability because a host with Git but no ripgrep is ordinary; the log index is a rebuildable projection with a repair that stays off the async pool; and a cross-panel link carries the correlation rather than the result, so the destination asks its own question with its own bounds. Registered in both tables of contents beside execution observability, which is the chapter it most depends on.

## 16. Required Verification

- [x] 16.1 Run `npm run lint:ci`, `npm run test`, and `npm run build`. All three green. Worth recording is that a full `vitest run` on this machine was intermittently red for several rounds while another session held the CPU with a Rust build — a different two or three tests each round, all failing at Testing Library's one-second `waitFor` default, none of them related to each other or to what was being changed. The two in this change's own suites were given explicit timeouts rather than shorter assertions, since the number was about the library's convenience and not about a load that is two queries deep. The rest were left alone: chasing a moving set of load-sensitive defaults across the repository is a different change.
- [x] 16.2 Run `npm run test:coverage` and keep the project coverage policy satisfied. 372 files, 2,053 tests, zero failures; statements 74.31%, branches 70.37%, functions 70.2%, lines 77.83%, and `coverage:policy:test` green. The known coverage-only lazy-route timeout did not reproduce.
- [x] 16.3 Run `npm run contracts:check` and resolve Tauri/Web service contract drift. Green with no drift. The one place drift would have been invisible is the shell runtime descriptor: it crosses the boundary as an internally tagged union, and losing the tag would keep both sides compiling while `kind` read undefined on the wire — which is why 15.8 put a guard on the attribute rather than trusting this command to notice.
- [x] 16.4 Run `npm run architecture:check` and resolve every context, visibility, service-boundary, direct-invoke, and production-file-size violation. Green. Two subtree budgets were raised during this group's work, each in the commit that needed it and each with the reason stated in place: `platform/database` twice, for the four-way version-collision fixture and for the no-backfill migration test.
- [x] 16.5 Run `npm run desktop:unit:test`. 18 passing, zero failures.
- [x] 16.6 Run `npm run test:desktop:session-workspace` against the real desktop artifact and record any platform-specific skip with an existing approved mechanism only. PASSED on Windows: three specs — every workspace tab's own content in the desktop runtime, terminal history read through the native record query, and the trace waterfall in both visual styles. Nothing was skipped, so no skip mechanism was invoked. The single-layer script assumes an artifact already exists and fails with a bare `ENOENT` on `latest-artifact.json` when it does not; `npm run test:desktop:build` first is what it wants, and it must be run through npm because the script requires `npm_execpath`. This result is Windows only and does not extend to macOS or Linux, which have their own runners.
- [x] 16.7 Run `npx playwright test` for Web/mock workspace behavior, responsive layouts, keyboard behavior, and both visual styles. 164 passing, zero failures. The first run found one real failure, and it was a stale assertion rather than a regression: `session-workspace-tabs.spec.ts` still expected Report's empty state, from before 10.9 moved Report onto the backend aggregate. The same test's Terminal History half had been updated for that move and the Report half had not. It now asserts what makes the report honest today — coverage per section, so a section nothing substantiates says `部分` while the rest reads `完整`, and an unreported token count renders as absent rather than as zero, which are different claims that a dash keeps apart and a `0` would not.
- [x] 16.8 Run `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check`. Clean.
- [x] 16.9 Run `cargo check --workspace`. Clean. `--workspace` rather than `--manifest-path`, which covers only `vanehub-ai` and silently skips the other members.
- [x] 16.10 Run `cargo clippy --workspace --all-targets -- -D warnings`. Clean, including the test targets, which is where `#[expect(dead_code)]` on newly-reachable code shows up and where `cargo check` alone stays green.
- [x] 16.11 Run `npm run native:panic:check`. Clean: no `unwrap`/`expect` in lib or bin targets.
- [x] 16.12 Run `cargo test --workspace`. Zero failures, twelve `test result: ok` lines — the count a healthy full run prints, which is worth stating because a run that ends early prints fewer and still looks green at a glance. Earlier rounds during this group intermittently failed two to five load-sensitive tests, all previously known: `code_intelligence::initialize_timeout_*`, the MCP relay socket-timing pair, and the `mcp_fixture_contracts` trio that spawns many node processes and loses to a concurrent `npm test`. Each passed in isolation and none shares a code path with this change.
- [x] 16.13 Run focused evidence, log-index, Shell, report, remote-provider, review-patch, and migration performance fixtures and record their bounded results. Twelve focused runs, all green: evidence 82, log-index repair store 18, log repair application 27, report aggregate 14, session queries (review patch render and revert) 29, review decisions 26, migrations 23, remote provider 81, session shell 34, log query service 4, log repair boundaries 9, evidence bridge architecture 12. The bounded results those fixtures now pin: retention stops at `RETENTION_BATCH` and drains on the next pass; log expiry stops at `REPAIR_PRUNE_ROWS`, which is also five hundred bound parameters in one statement now that each expired row is named; the retention delete seeks the expired window and reaches the journal through its session index with no `SCAN` in the plan; and the report aggregate query uses its index rather than scanning the record table.
- [x] 16.14 Run `openspec validate upgrade-session-workspace-evidence-console --strict`. Valid.
- [x] 16.15 Run `openspec validate --specs --strict`. 136 items, zero failures.
- [x] 16.16 Review `git diff --check`, ensure no generated artifacts/secrets/test SSH credentials were committed, and confirm every completed task has corresponding test evidence before marking it complete. `git diff --check` clean against `main`. No `dist/`, `target/`, `node_modules/`, `test-results/`, or `coverage/` path appears in the diff. A key-material scan surfaced three hits and all three are redaction *inputs* in `_tests.rs` files — `SENTINELKEYmmm`, `echo -----BEGIN RSA PRIVATE KEY-----`, and `not-a-real-key` — each present to prove that shape gets redacted, which is the one place the string legitimately belongs. Four filename hits on "token" are usage tokens and visual design tokens. On evidence for completed tasks: every tick in groups 12 through 16 names what was run or what was found, and where a rule was added it says how it was proved non-vacuous — by removing the thing it guards and confirming the guard names it. Two ticks state a limit rather than a result: 16.6 is Windows only, and the `test:desktop:cli-terminal` layer was not run at all and is not claimed.
