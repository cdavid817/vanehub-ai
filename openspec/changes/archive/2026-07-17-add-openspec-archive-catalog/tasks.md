## 1. Catalog Generation

- [x] 1.1 Extend the archive index generator to create deterministic JSON entries from archive directory structure.
- [x] 1.2 Regenerate the JSON catalog and human-readable README for all online archives.
- [x] 1.3 Document catalog-first archive lookup in contributor guidance.

## 2. Verification

- [x] 2.1 Confirm every online archive has one catalog entry and capabilities match delta-spec paths.
- [x] 2.2 Run `openspec validate add-openspec-archive-catalog --strict` and `openspec validate --specs --strict`.
