## 1. Loop Center panel treatment

- [x] 1.1 Reproduce current state: run `npm run dev`, open Loop Center in both `futuristic` and `minimal` styles, and confirm the three columns render without panel/card backgrounds or borders
- [x] 1.2 Apply the shared `.ucd-panel` / `.ucd-muted-panel` / `.ucd-card` treatment (or the closest existing shared class) to the Loop Center nav column, main content column, and inspector column in `src/loop-center/loop-center.tsx` and any child components that own their own wrapper markup
- [x] 1.3 Verify both registered styles render with visible panel separation and no clipped/overlapping content across Loop Center's definition, run-record, and inspector states
- [x] 1.4 Update the `tests/e2e/loop-engineering.visual.spec.ts` baseline if the panel treatment changes pixel output, and review the diff intentionally before accepting it (no stored pixel baseline exists — test is geometry/bounds-based; ran it and confirmed all 4 style/width variants pass)

## 2. Settings section-group headers

- [x] 2.1 Identify the existing section-header pattern used by Basic Settings (`SectionPanel`/equivalent in `src/settings/pages/page-parts.tsx`) — `SectionPanel`'s bordered header-divider is designed for full card sections, not a label above a card grid; used the same `border-b` divider + label/count idiom already used by the session sidebar's pinned-group header for a better shape fit
- [x] 2.2 Replace the `-- 用户配置 --` / `-- 项目配置 --` raw-text separators in `src/settings/pages/mcp-page.tsx` with that shared pattern
- [x] 2.3 Confirm the new labels are sourced from translation resources rather than new hard-coded literals (unchanged `t("mcp.group.user")`/`t("mcp.group.project")`, values already clean without dashes)
- [x] 2.4 Visually verify the MCP settings page in both `futuristic` and `minimal` styles

## 3. Workspace top bar search at narrow widths

- [x] 3.1 Reproduce: resize the workspace to 900px and confirm the top bar search control disappears with no replacement affordance
- [x] 3.2 Implement a fix in `src/main-layout/top-bar.tsx` so search stays reachable at widths at or below 900px (icon-collapse: a `lg:hidden` search icon button toggles a full-width input+close-button header state)
- [x] 3.3 If icon-collapse is used, give the collapsed control a translated accessible name/tooltip consistent with existing icon-only controls (e.g. activity bar entries) — added `layout.openSearch`/`layout.closeSearch` keys to all 5 registered locales
- [x] 3.4 Verify search remains usable by pointer and keyboard at 900px and at 640px without overlapping or clipping adjacent top bar controls
- [x] 3.5 Add or extend an e2e check asserting the search control (or its icon-collapsed equivalent) is reachable at widths at or below 900px — extended `tests/e2e/workspace-activity-bar.spec.ts`'s existing 900px/640px parameterized test

## 4. Toast dismiss resilience to navigation

- [x] 4.1 Reproduce directly with a stopwatch: trigger a toast, navigate across several tabs/routes during its lifetime, and confirm whether the dismiss timer is genuinely reset/blocked or just slower than the manual screenshot cadence made it look — timed reproduction (toast appear +1.4s, still visible after 4 tab switches at +3.6s, hidden at +7.0s ≈ 5.6s after appearing) shows the timer already fires correctly on schedule; the original "lingering" impression was the manual screenshot cadence, not a code bug
- [x] 4.2 If confirmed as a timer-ownership bug, move toast timer/creation state ownership to the notification provider — not applicable, no bug found; timer is already owned correctly (`useEffect` keyed on stable `notification.id` inside a component that isn't remounted by in-workspace tab navigation)
- [x] 4.3 Confirm the existing bounded-history/eviction behavior is unaffected by the ownership change — not applicable, no ownership change made
- [x] 4.4 Add or extend a test covering "toast dismisses on schedule even when the user navigates during its lifetime" — added to `tests/e2e/notifications.spec.ts` as a permanent regression test

## 5. Agent Terminal web-mock single-locale notice

- [x] 5.1 Locate the duplicated Chinese+English "local CLI unavailable" notice in the Web/mock Agent Terminal path — two separate sources both writing into the same xterm buffer: `agent-terminal-tab.tsx` writes the proper localized `sessionTabs.agentTerminal.simulatedBanner` line, then `web-agent-client.ts`'s `openAgentTerminal` separately emitted a hardcoded English `output` event ~30ms later
- [x] 5.2 Replace both literal lines with a single translation-resource-driven string — removed the redundant hardcoded `output` event from `web-agent-client.ts` entirely (mock process output, not a UI label) since the existing localized banner already communicates the same notice; no new translation keys needed
- [x] 5.3 Verify `src/i18n/i18n-resource-parity.test.ts` and the hard-coded-visible-text guardrail still pass (also updated `web-agent-client.test.ts`'s event-order assertion, which had encoded the old duplicated-event behavior as expected)

## 6. Session info panel row treatment

- [x] 6.1 Replace the bordered/background wrapper in `Field` and `UsageMetric` (`src/main-layout/session-info-panel.tsx`) with unframed rows separated by dividers or spacing — `Field` now uses a `border-b` divider (mirroring the same pattern already used by `loop-inspector.tsx`'s own `Field`), `UsageMetric` and `EmptyState` (same file, same nested-card issue) dropped their border/background entirely
- [x] 6.2 Verify the "基本信息" and "Token 使用" tabs remain visually scannable in both `futuristic` and `minimal` styles after removing the inner card borders

## 7. Session sidebar control consolidation

- [x] 7.1 Consolidate the always-visible "批量管理"/"归档" row in `src/main-layout/session-sidebar.tsx` into a compact overflow control — added a `role="menu"` popover (mirroring the existing pattern in `folder-opener-control.tsx`) triggered by a new "更多操作" icon button next to the title, freeing one full-width row; the trigger shows a `text-primary` cue when the archived view is active so state stays visible while the menu is closed
- [x] 7.2 Confirm both actions remain reachable in one interaction and keep existing accessible names/tooltips — added `layout.sessionMoreActions` to all 5 locales; menu items reuse the existing translated labels
- [x] 7.3 Verify the session list gains the freed vertical space and no existing e2e test (e.g. batch-management flows) breaks — updated `workspace-activity-bar.spec.ts` and `session-sidebar.test.tsx` for the new menu structure (both were the only references to the old always-visible buttons); manually verified the full open-menu → batch-mode → select-all → delete-confirmation flow still works end to end

## 8. Basic Settings advanced-config nested cards

- [x] 8.1 Add a `"plain"` variant to `SectionPanel` in `src/settings/pages/page-parts.tsx`: icon/title/description header with a bottom divider, no outer border/background/rounding
- [x] 8.2 Switch `NetworkProxySection`, `DataManagementSection`, `LogManagementSection`, and `basic-settings-page.tsx`'s local `NodeEnvironmentPanel` from `variant="settings"` to `variant="plain"` (also dropped each one's now-redundant inner `p-5 sm:p-6` content padding, previously sized to align with the removed card border)
- [x] 8.3 Remove the now-unneeded `gap-5` from `SettingsDisclosure`'s children wrapper (each `"plain"` section manages its own spacing); confirmed this doesn't affect the Skills page's single-child use of `SettingsDisclosure` (gap was a no-op there with only one child)
- [x] 8.4 Visually verify "高级配置" expanded in both `futuristic` and `minimal` styles: sub-topics read as one grouped list, not cards inside a card — confirmed via screenshots, no more double-border artifact

## 9. Folder-openers disclosure unification

- [x] 9.1 Replace `FolderOpenersSection`'s local `<details>`/`<summary>` block with `SettingsDisclosure` (added a `folderOpeners.manageDescription` key across all 5 locales, since `SettingsDisclosure` requires a description and the original `<details>` had none)
- [x] 9.2 Verify the "管理工作区打开工具" panel still opens/closes correctly and its content (opener checkboxes, reorder buttons, refresh) is unaffected — confirmed via screenshot and `basic-settings-page.test.tsx`
- [x] 9.3 Visually verify against the "高级配置" disclosure for consistent padding/typography in both styles — confirmed, both now share identical header treatment

## 10. CLI Management duplicate upgrade-all text

- [x] 10.1 Remove the redundant `<span>{t("cli.upgradeAll", ...)}</span>` from the "本地环境检查" info row in `src/settings/pages/providers-page.tsx`, keeping the real action button in the page header
- [x] 10.2 Visually verify the "本地环境检查" row in both `futuristic` and `minimal` styles: no orphaned trailing text, row still reads cleanly — confirmed via screenshots; `providers-page.test.tsx` (10 tests) and lint still pass

## 11. CLI Parameters sticky agent picker

- [x] 11.1 Add `sticky top-4 self-start` to the "受管 CLI" `SectionPanel` in `src/settings/pages/cli-parameters-page.tsx`
- [x] 11.2 Verify the agent picker stays visible while scrolling through Codex CLI's 6-parameter list, and that switching agents from a scrolled position still works correctly — confirmed via a scripted scroll + click-while-scrolled test; screenshots show the picker pinned at `top-4` with later parameter cards scrolling past it, and switching to Gemini CLI mid-scroll correctly refreshed the right column while the picker stayed in place
- [x] 11.3 Visually verify in both `futuristic` and `minimal` styles, and at the `lg` responsive breakpoint where the layout collapses to a single column — `cli-parameters-settings.spec.ts` (2 tests, including a narrow-viewport minimal-theme case) passes; `cli-parameters-page.test.tsx` passes

## 12. MCP dialog accessibility

- [x] 12.1 Migrate `src/settings/pages/mcp/mcp-server-form.tsx` from its hand-rolled overlay to `ApplicationDialog`; keep the existing "取消"/"保存" footer buttons, drop the now-redundant header title/close button, and add `data-dialog-autofocus` to the name field
- [x] 12.2 Migrate `src/settings/pages/mcp/mcp-import-export.tsx` from its hand-rolled overlay to `ApplicationDialog`; replace the icon-only close button with a visible "关闭" button (reusing the existing `mcp.form.close` label) since this dialog has no other explicit close affordance, and add `data-dialog-autofocus` to the import textarea
- [x] 12.3 Add `aria-label` to the server card's enable/disable toggle button in `src/settings/pages/mcp/mcp-server-card.tsx` — went further than the original `title` text once a multi-server ambiguity became apparent (see design.md decision 12 note); added `mcp.toggle.disableNamed`/`enableNamed` interpolated keys across all 5 locales (`{{name}}`-aware, matching the existing `cli.refreshOne` convention) and retired the old unnamed, now-dead `mcp.toggle.disable`/`enable` keys
- [x] 12.4 Verify with a scripted check: `role="dialog"` is present, Escape closes both dialogs, and Tab cycling stays within the dialog — confirmed via automated checks (dialog count, aria-labelledby text, Escape behavior) and codified as a permanent regression test in `tests/e2e/mcp-settings.spec.ts`
- [x] 12.5 Visually verify both dialogs in `futuristic` and `minimal` styles; confirm existing MCP page tests still pass — `mcp-user-visible.test.tsx`, `i18n-visible-text-guardrail.test.ts`, and `i18n-resource-parity.test.ts` pass; new `mcp-settings.spec.ts` (2 tests) passes against a freshly-started dev server (an earlier run against this session's long-lived dev server gave false failures — same stale-server lesson noted in task group 13 below)

## 13. Agent Configurations: OnePiece tab parity

- [x] 13.1 Remove `OnePieceConfigurationPanel`'s outer `SectionPanel` wrapper (and its redundant "OnePiece" title/description) in `src/settings/pages/agents/onepiece-configuration-panel.tsx`, replacing it with the same flat `<div className="space-y-4">` structure `AgentGlobalConfigPanel` uses, so switching tabs no longer shows a card border appearing/disappearing
- [x] 13.2 Add a local "search saved profiles" input to the OnePiece tab's toolbar row, matching `CliConfigToolbar`'s layout and reusing the existing generic `agentConfigurations.toolbar.search` translation key (no new i18n keys needed); combine it with the page-level `searchTerm` prop the same way `AgentGlobalConfigPanel` already does
- [x] 13.3 Remove the now-orphaned `onepiece.description` key from all 5 locales (`zh-CN`, `en`, `ja`, `ko`, `zh-TW`) after confirming via grep it has no other callers; keep `onepiece.title` since it still drives the tab label
- [x] 13.4 Update the two tests that depended on the removed structure: `onepiece-configuration-panel.test.tsx` needed no changes (none referenced the removed heading), but `agent-configurations-page.test.tsx` and `tests/e2e/onepiece-agent.spec.ts` both located the panel via the now-gone "OnePiece" heading/`<section>` ancestor — updated both to scope via the tabpanel directly (simpler than the original xpath-ancestor lookup)
- [x] 13.5 Visually verify all four tabs in both `futuristic` and `minimal` styles: OnePiece now matches its siblings' flat structure with no outer border/shadow; also verified at 390px width (no horizontal overflow, toolbar collapses to the same `grid-cols-2 gap-2 sm:flex` pattern `CliConfigToolbar` already uses) and confirmed the new search input filters as expected via the existing unit test coverage

## 14. Skill Management page fixes

- [x] 14.1 Unify the skill-list container treatment: `LifecycleSkillRows` (rendered for the "全部"/"未分配" views) used a plain `border border-border bg-background` wrapper while `SkillSelectionPanel` (rendered for a specific-Agent view) and the empty state both use `.ucd-panel` — switched `LifecycleSkillRows`'s wrapper in `src/settings/pages/skills/skill-card-list.tsx` to `.ucd-panel` so the container no longer changes visual weight/texture depending on which nav item is selected
- [x] 14.2 Restore the local "search saved Skills" input in `SkillFilterToolbar` (`src/settings/pages/skills/skill-filter-toolbar.tsx`), reusing the already-defined-but-orphaned `skills.filters.search`/`skills.filters.searchPlaceholder` translation keys and wiring it to the existing `filters.query` state (already consumed by `filterGlobalSkillInventory`, just never had an input control) — see design.md decision 14 for why this required pausing to confirm with the user before implementing, unlike every other task group in this change
- [x] 14.3 Update the tests that encoded the box's prior absence as expected behavior: `skills-page.test.tsx`'s `not.toContain("搜索 Skill")` assertion flipped to assert the new placeholder text renders; `tests/e2e/skills-settings.spec.ts`'s `not.toHaveCount(0)` assertion on the same placeholder replaced with a real interaction test (fill the local search box, confirm the result count updates)
- [x] 14.4 Visually verify both fixes in `futuristic` and `minimal` styles: the "全部 Skill" list and the per-Agent assigned/available panels now render the same panel texture; the new search box filters correctly and sits cleanly in the existing filter-toolbar grid at desktop and 390px widths (no horizontal overflow)
- [x] 14.5 Investigated and intentionally left unchanged: `SkillAgentMountPathsPanel`'s single mount-path row renders inside its own `rounded-md border` box nested inside the "Agent 挂载路径" disclosure's tinted content area. Unlike the Basic Settings advanced-config case (task group 8, four stacked cards), this is exactly one item, not repeated rows, so it doesn't violate the "no nested card-in-card" scenario's actual scope (repeated rows each getting their own card); the border also usefully groups the path input with its save button. Judged too weak a case to spend a "plain"-variant migration on this round.

## 15. Prompt Hook dialog accessibility

- [x] 15.1 Migrate all four Prompt Hook overlay states to the shared `ApplicationDialog` component: `src/settings/pages/prompt-hooks/prompt-hook-dialogs.tsx`'s hand-rolled `Modal` (backing the preview, delete-confirm, and create/edit forms) and `src/settings/pages/prompt-hooks/prompt-hook-lifecycle-panel.tsx`'s hand-rolled overlay (the "打开草稿、版本与效果评估" advanced panel) — confirmed by a scripted probe that, pre-fix, `role="dialog"` count was 0 and pressing Escape did not close the create dialog; post-fix both are 1/closes correctly
- [x] 15.2 Add an explicit visible close affordance to the two overlays that previously relied solely on their now-removed hand-rolled header close button and had no other cancel-style action in their body: a "关闭" button added to the preview dialog's footer and to the top of the lifecycle panel's body (reusing the existing `promptHooks.dialog.close` key — no new i18n needed)
- [x] 15.3 Add `data-dialog-autofocus` to the first meaningful editable field in each dialog: the "名称" field in the create/edit form (not "ID", since that field is disabled in edit mode and can't receive focus), the template-body textarea in the lifecycle panel, and the new close button in the preview dialog (which has no editable fields)
- [x] 15.4 Verify no existing test encoded the old hand-rolled structure as expected behavior — `prompt-hooks-page.test.tsx`, `prompt-hooks-page.interaction.test.tsx`, and `prompt-hook-lifecycle-panel.test.tsx` (9 tests total) all passed unchanged
- [x] 15.5 Add `tests/e2e/prompt-hooks-settings.spec.ts` (2 tests, new file — this page had no prior e2e coverage): create/preview-assembly dialogs close on Escape with correct autofocus; a user-created hook's edit/advanced-lifecycle/delete dialogs are all real dialogs, ending with actually deleting the hook as cleanup
- [x] 15.6 Investigated and intentionally left unchanged: the per-Agent CLI-binding checkboxes on each hook card (`prompt-hook-card-list.tsx`) each render inside their own `rounded-md border` chip. Unlike the passive display rows the "no nested card-in-card" rule targets, these are interactive checkbox targets where a distinct clickable boundary is a reasonable, common pattern for a multi-select chip group, not decorative redundancy — left as-is.

## 16. Prompt Hook filter toolbar layout

- [x] 16.1 Fix the filter toolbar's premature multi-column breakpoint: `src/settings/pages/prompt-hooks/prompt-hook-filter-toolbar.tsx`'s 5-column grid (4 selects + search) activated at `lg` (1024px), but the settings content column at that width doesn't have enough room, squeezing the search input to the point of truncating its own placeholder text — confirmed via screenshots at 1024px/1280px. Changed the breakpoint from `lg:` to `xl:` (1280px), which also now matches the breakpoint `PromptHookCardList` already uses for its own 1→2 column switch, so the toolbar and card grid go "wide" at the same point instead of transitioning at two different widths
- [x] 16.2 Move the "当前显示 N / M 个 Prompt Hook" result-count summary in `src/settings/pages/prompt-hooks-page.tsx` from after the full card list to immediately after the filter toolbar, so adjusting a filter shows the new count without scrolling past however many cards are currently rendered — matches where Skills page's equivalent count already sits (inside its toolbar)
- [x] 16.3 Re-verified `prompt-hooks-page.test.tsx`/`prompt-hooks-page.interaction.test.tsx` (7 tests) needed no changes; added a third e2e test to `tests/e2e/prompt-hooks-settings.spec.ts` asserting the search input stays >300px wide with no page overflow at 1024px and that the result-count element sits above the first card
- [x] 16.4 Visually verified at 900/1024/1280/1440px in both styles: no horizontal overflow at any width (confirmed both before and after — the pre-fix issue was cramping, not overflow), toolbar stacks cleanly below `xl` and matches the card grid's column layout at/above it

## 17. SSH Connections page fixes

- [x] 17.1 Migrate `SshConnectionForm`'s hand-rolled `<div className="fixed inset-0 ...">` overlay (`src/settings/pages/ssh/ssh-connection-form.tsx`) to `ApplicationDialog` — confirmed via the same scripted probe used for MCP/Prompt Hook: pre-fix `role="dialog"` count was 0 and Escape did not close the dialog; post-fix both are fixed
- [x] 17.2 Fix the connection card's test-status badge (`src/settings/pages/ssh/ssh-connection-card.tsx`): it used a hand-rolled `<span>` with class `ucd-status-info`, which does not exist anywhere in `src/styles.css` (grep-confirmed) — the badge rendered with a fully transparent background regardless of status (confirmed via computed-style probe: `rgba(0, 0, 0, 0)` in both the "not-tested" and "succeeded" states). Replaced with the shared `Badge` component (already used by every other status indicator in the app) and a `testStatus → tone` map (`not-tested` → muted, `succeeded` → success, `failed` → danger)
- [x] 17.3 Fix premature validation in `SshConnectionForm`: all seven fields' validation errors were computed unconditionally from initial (empty) state and rendered immediately when the dialog opened, before any user interaction — confirmed via screenshot showing every field's error text visible on a completely untouched "新增 SSH 连接" dialog. Added a `submitted` flag (set on first submit attempt); error text now only renders after that, while the underlying validation/disabled-button logic is unchanged once triggered. The Save button is no longer disabled before the first attempt, so clicking it while empty now correctly reveals which fields are missing instead of doing nothing
- [x] 17.4 Added `data-dialog-autofocus` to the "名称" (name) field, matching the established first-editable-field autofocus convention from the other dialog-accessibility fixes this change
- [x] 17.5 No existing test encoded the old hand-rolled structure, the missing badge color, or the premature validation as expected behavior — full unit suite (517/517) passed unchanged. This page had no prior e2e coverage; added `tests/e2e/ssh-connections-settings.spec.ts` (2 tests): dialog role/autofocus/deferred-validation/Escape-close, and a full create-then-test flow asserting both status badges render with a non-transparent background color
- [x] 17.6 Visually verified in both `futuristic` and `minimal` styles: the add-connection dialog now opens with proper focus/dimming and no error text; the "未测试"/"成功" badges now render with visible muted-gray/green backgrounds instead of blending into the card

## 18. Extensions page fixes

- [x] 18.1 Migrate `InstallPreview`'s hand-rolled overlay (`src/settings/pages/extensions-page.tsx`) to `ApplicationDialog`. Unlike the fully-broken dialogs fixed in task groups 12/15/17, this one already had `role="dialog"`/`aria-modal="true"` set correctly — confirmed via probe that the remaining gap was purely interactive: focus never moved into the dialog on open (stayed on the trigger button) and Escape did not close it
- [x] 18.2 Fix nested card-in-card: the framework list was wrapped in a `SectionPanel` (bordered "card" variant) whose own children are each an independently-bordered `.ucd-panel` card — confirmed by screenshot in `minimal` style showing a clear double border/shadow. Switched to `variant="plain"`, the same fix already used for Basic Settings' advanced-config sub-panels (task group 8)
- [x] 18.3 Fix `StatusPill` (`src/settings/pages/page-parts.tsx`): it inferred its color tone by checking whether the *already-translated* status string contained the English substrings `"Disabled"`, `"Error"`, or `"Update"` — meaning in any non-English locale (this app's primary usage locale is zh-CN) essentially every status fell through to the `"success"` (green) default, including error and update-available states. Confirmed by screenshot: extensions in the "当前环境不支持" (unsupported) state rendered with a green pill. Changed `StatusPill` to take an explicit required `tone` prop (matching `Badge`'s existing convention) instead of sniffing rendered text, and added a `status → tone` map at both of `StatusPill`'s call sites (`extensions-page.tsx`'s `ExtensionLifecycleStatus`, and `sdk-page.tsx`'s `SdkInstallStatus` — found via grep while locating all callers; not a page the user asked about this round, but making `tone` a required prop meant TypeScript would not compile until every caller was fixed, and leaving the shared component's root cause broken for one known caller while fixing it for another wasn't a real option)
- [x] 18.4 `npx tsc --noEmit` confirmed no other caller of `StatusPill` exists beyond these two, and both were updated correctly
- [x] 18.5 No existing test encoded the broken dialog interactivity, the nested-card styling, or the broken status colors as expected behavior: `extensions-page.test.tsx` (2 tests) passed unchanged; full unit suite (517/517) passed unchanged. Extended the existing `tests/e2e/extensions-settings.spec.ts`: its dialog test now also asserts focus lands in the dialog and Escape closes it; added a new test asserting the status badge's computed background is not transparent
- [x] 18.6 Visually verified in both `futuristic` and `minimal` styles: "本地框架" no longer double-borders its cards; the "当前环境不支持" badge now renders muted-gray instead of green; the install-preview dialog opens with a dimmed backdrop, moves focus into the dialog, and closes on Escape

## 19. Plugin Integrations nested card

- [x] 19.1 Same nested card-in-card shape as task group 18's Extensions fix, found on the sibling "插件集成" page: `plugin-integrations-page.tsx` wraps its "内置集成" integration list in a `SectionPanel` (bordered "card" variant) whose children are each an independently-bordered `.ucd-panel` card — confirmed by screenshot in `minimal` style. Switched to `variant="plain"`, same fix
- [x] 19.2 No other issues found on this page: it has no dialogs (confirmed — no `plugin-integrations/` sub-directory, no modal/overlay code at all), and its own `statusTone()` function already maps status **values** (not translated text) to tone correctly and exhaustively across all 5 `PluginIntegrationStatus` values — this page did not have the `StatusPill`-style locale bug
- [x] 19.3 `plugin-integrations-page.test.tsx` (3 tests) passed unchanged; full unit suite (517/517) passed unchanged. No new e2e test added — this is a pure CSS/structural change with no new interactive behavior to guard, unlike the dialog/badge-color fixes in other task groups this change
- [x] 19.4 Visually verified in both `futuristic` and `minimal` styles at 430/768/900/1024/1440px: no horizontal overflow at any width (unchanged from before), and the "内置集成" section no longer double-borders its one integration card

## 20. Execution Observability page fixes

- [x] 20.1 Same nested card-in-card shape as task groups 18/19: `observability-settings-page.tsx`'s "MCP 观测能力" section wraps a grid of per-agent capability chips (`claude-code`/`codex-cli`/`gemini-cli`/`opencode`, each `.ucd-panel`) inside a `SectionPanel` (bordered "card" variant) — confirmed by full-page screenshot in both styles. Switched to `variant="plain"`, same fix as the last two rounds
- [x] 20.2 The other three sections on this page (本地时间线/OTLP 导出/采集与隐私) each nest exactly one *single, static* bordered info box (a restart notice, a capture-policy hint) inside their own `SectionPanel` card — deliberately left unchanged. This matches the established distinction from task group 17's SSH mount-paths note: the "no nested card-in-card" rule targets **repeated** rows/items each claiming their own card, not a single static note; a lone info box inside a card is the same shape already accepted elsewhere in this app (e.g. every `operationError`/`notice` banner across the settings pages touched this change)
- [x] 20.3 While fixing the nested card, also noticed and fixed a smaller, unrelated inconsistency: "MCP 观测能力" was the only one of the page's four `SectionPanel`s with no `icon` prop, so it had no icon avatar next to its title. Added `icon={Boxes}` (matching the icon already used for the "MCP 服务器" settings page in the sidebar, for the same underlying MCP concept) — confirmed `variant="plain"` + `icon` is an already-established combination on this codebase (used by 4 sections in Basic Settings' advanced-config disclosure), so this doesn't introduce a new pattern, though it does mean this section's icon avatar renders slightly smaller/differently-tinted than its three "card"-variant siblings on the same page — an inherent, accepted trade-off of `variant="plain"`'s existing styling, not something newly introduced by this fix
- [x] 20.4 `observability-settings-page.test.tsx` (2 tests) and the existing `tests/e2e/execution-observability.spec.ts` (2 tests) both passed unchanged — this page's e2e coverage exercises the narrow-viewport defaults and a separate session-level trace timeline, neither of which touches the capability-chip styling; full unit suite (517/517) passed unchanged. No new e2e test added, matching task group 19's reasoning: a pure CSS/structural change with no new interactive behavior to guard
- [x] 20.5 Visually verified in both `futuristic` and `minimal` styles: "MCP 观测能力" no longer double-borders its four capability chips and now has an icon avatar consistent with its sibling sections

## 21. Usage Statistics page fixes

- [x] 21.1 Same nested card-in-card shape as task groups 18–20, found in two places on this page: `usage-trend.tsx`'s "每日趋势" section nests its two `TrendLine` charts (真实 Token / 估算字符) inside a `SectionPanel` card, and `usage-agent-breakdown.tsx`'s "Agent 使用量" section nests its per-agent `<article>` rows inside another `SectionPanel` card — both confirmed by full-page screenshot in both styles. Switched both to `variant="plain"`, the same fix as the last three rounds. Neither of these two `SectionPanel`s passed an `icon` prop before or after, so unlike task group 20 there was no icon-consistency trade-off to weigh here
- [x] 21.2 `usage-accounting-note.tsx`'s "统计口径" section, which nests two boxes (an explanatory note and a "更新于" timestamp) inside its own `SectionPanel`, was deliberately left unchanged: unlike the two fixes above, these two boxes are not a dynamically-repeated list of same-shaped items — they're a fixed pair of distinct, differently-shaped, differently-purposed elements (one wide multi-line note, one narrow single-line timestamp) arranged in an asymmetric `minmax(0,1fr)_auto` grid, the same shape as the `operationError`/`notice` banner pairs already accepted throughout this app's settings pages
- [x] 21.3 Separately, `usage-statistics-page.tsx`'s two-column grid (`UsageTrend` next to `UsageAgentBreakdown`) had no `items-start`, so the default `align-items: stretch` forced the shorter "Agent 使用量" column to stretch to match the taller "每日趋势" column's height — screenshot showed a large empty gap below the two agent rows before the next section began. Added `items-start` so each column sizes to its own content
- [x] 21.4 `usage-statistics-page.test.tsx` (8 tests) and the existing `tests/e2e/usage-statistics.spec.ts` (3 tests) both passed unchanged; full unit suite (517/517) passed unchanged. No new e2e test added, matching task groups 19/20's reasoning: a pure CSS/structural change with no new interactive behavior to guard
- [x] 21.5 Visually verified in both `futuristic` and `minimal` styles: "每日趋势" and "Agent 使用量" no longer double-border their inner rows, and the empty space below "Agent 使用量" is gone, so "统计口径" now starts immediately after instead of being pushed down by a stretched, mostly-empty column

## 22. Verification

- [x] 22.1 `npm run lint` — clean
- [x] 22.2 `npm run test` — 129 files / 517 tests passed
- [x] 22.3 `npm run build` — unblocked: this worktree's `node_modules` had been installed by `pnpm` at some point (stray untracked `pnpm-lock.yaml`, `node_modules/.pnpm/...` layout), contradicting the project's npm-only rule; removed both and ran a clean `npm install` (527 packages, `package-lock.json` unchanged), then `npm run build` — passes (exit 0, `✓ built in 5.90s`); re-ran `npm run test` afterward as a sanity check on the fresh install — still 129 files / 517 tests passed
- [x] 22.4 `cargo check --manifest-path src-tauri/Cargo.toml` — passes cleanly. Also ran the broader `cargo test --manifest-path src-tauri/Cargo.toml` per project.md's standard gate: 1209 passed / 0 failed / 9 ignored, plus the `architecture`/`mcp_fixture_contracts`/`mcp_relay_provider_invocations` integration suites all green. One transient failure on the first attempt (`legacy_sse_fixture_contract_is_complete`, empty stdout/stderr) turned out not to be a real bug: `git diff -- src-tauri/` confirms zero actual Rust changes in this worktree (matching this change's "no backend/Rust changes" scope), the same test passed standalone (`node tests/fixtures/mcp_fixture_probe.cjs legacy-sse`), passed running just that test file in isolation, and passed again on an immediate full-suite re-run — consistent with a timing-sensitive test (tight internal timeouts down to 100ms, several chained child-process spawns) occasionally flaking under ~1200-test parallel load, not something this change introduced
- [x] 22.5 Manual visual QA pass for the Usage Statistics fix specifically, plus `tests/e2e/usage-statistics.spec.ts` (3 tests) — all pass against a freshly-started dev server; a full-suite e2e re-run across every task group is still pending the build fix
- [x] 22.6 `openspec validate polish-workspace-visual-consistency --strict` — valid
