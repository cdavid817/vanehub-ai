## 1. Project-slug lookup and session file resolution

- [x] 1.1 Added `find_gemini_chat_session_since(working_directory, since_ms)` in `infrastructure/providers/session_capture.rs`, mirroring `find_opencode_session_since`/`find_codex_rollout_since`: reads `~/.gemini/projects.json` via `read_gemini_project_slug`, lists `.jsonl` files under `~/.gemini/tmp/<slug>/chats/`, filters by mtime >= `since_ms` (2-second grace buffer, matching the existing pattern), returns the most-recently-modified match.
- [x] 1.2 Added `gemini_home_dir()` (`~/.gemini`, no env var override — verified no such override exists in the installed package's source) and `gemini_projects_registry_path()` (`~/.gemini/projects.json`).
- [x] 1.3 Unit tests in `session_capture_tests.rs` (via the `pub(super)` `read_gemini_project_slug` helper, which takes an explicit registry path so it's testable without touching the real home directory): `gemini_project_slug_resolves_a_matching_registered_path`, `gemini_project_slug_returns_none_for_an_unregistered_path`, `gemini_project_slug_missing_registry_file_is_graceful`. Path matching reuses the existing `paths_match()` helper (already slash/case-insensitive on Windows), so no separate Windows-case-variance test was needed — that behavior is already covered by `paths_match`'s own existing test coverage from the codex-cli/opencode capture tests. `find_gemini_chat_session_since` itself is intentionally left without a direct unit test, matching the existing precedent: neither `find_codex_rollout_since` nor `find_opencode_session_since` has one either, since all three depend on real home-directory resolution that isn't practically mockable without a larger refactor; covered instead by 4.6's manual verification task.

## 2. Usage aggregation

- [x] 2.1 Added `aggregate_gemini_usage(path, file)` in `infrastructure/terminal_usage_ingestion.rs`: parses each JSONL line via a permissive `GeminiChatMessageLine { type, tokens }` struct (metadata/`$set` lines deserialize successfully with both fields `None` since serde ignores their unrecognized keys, so they fall through the `type == "gemini"` filter naturally — no special-casing needed), sums `tokens.input` into `input_tokens`, `tokens.output + tokens.thoughts` into `output_tokens`, `tokens.cached` into `cache_read_tokens`; `cache_creation_tokens` stays 0. `tokens.tool` is deliberately left unmapped (see design.md Decision 3).
- [x] 2.2 Added `ingest_gemini_terminal_usage(session_folder, started_at_ms, sessions, message_id, session_id, agent_id, clock)` following the exact same shape as `ingest_opencode_terminal_usage`/`ingest_codex_terminal_usage`.
- [x] 2.3 Fixture-based unit tests pinned to the exact shape read from `ChatRecordingService` source: `gemini_usage_sums_across_all_gemini_messages_and_folds_thoughts_into_output` (metadata line + `type: "user"` message + two `type: "gemini"` messages + a `$set` line, asserting only the two gemini messages contribute and `thoughts` folds into output), `gemini_usage_skips_gemini_messages_without_a_tokens_object`, `gemini_chat_session_without_any_gemini_messages_yields_zero_totals`.

## 3. Terminal lifecycle wiring

- [x] 3.1 Added `"gemini-cli"` to the placeholder-creation `matches!` in `terminal_process.rs` (now `"claude-code" | "opencode" | "codex-cli" | "gemini-cli"`).
- [x] 3.2 Added a `"gemini-cli"` arm to `run_terminal_usage_ingestion`'s dispatch, calling `ingest_gemini_terminal_usage`.
- [x] 3.3 Confirmed the existing `matches!(request.agent.id.as_str(), "codex-cli" | "opencode")` gate for the live `ProviderSessionCapture` discovery poll (a separate, earlier mechanism in `terminal_process.rs`) correctly stays unchanged/gemini-cli-excluded — that mechanism is codex-cli/opencode-specific by design, not something gemini-cli needs or should join.

## 4. Verification

- [x] 4.1 `cargo test --lib` — 748 passed, 0 failed, 2 ignored (pre-existing) — up from 742 (6 new tests: 3 in `session_capture_tests.rs`, 3 in `terminal_usage_ingestion.rs`).
- [x] 4.2 `cargo clippy --lib -- -D warnings` — clean.
- [x] 4.3 `cargo test --lib --test architecture` (isolated cold-build target dir, to avoid the locked dev-app binary) — DDD boundary checks pass.
- [x] 4.4 `openspec validate --specs --strict` — 71 passed, 0 failed. `openspec validate add-gemini-cli-terminal-usage-tracking --strict` — valid (a MODIFIED delta was added — see proposal.md Capabilities — after discovering the project's CI enforces "every unarchived change must have at least one delta" more strictly than a spec-text-only reading of AGENTS.md's `--skip-specs` archive-time allowance suggested).
- [ ] 4.5 **Needs the user's own machine**: manual end-to-end smoke test — run a real gemini-cli interactive terminal session in the app, confirm the Token Usage panel shows non-zero reported tokens consistent with `~/.gemini/tmp/<slug>/chats/*.jsonl`'s actual content. Not completed during implementation: a live probe (`gemini -p "..."` using the machine's existing OAuth credentials, run directly since credentials are global to the user account) was attempted but did not finish producing a sample in the available time. **Retried 2026-08-06**: confirmed `oauth_creds.json`/`projects.json` are real and present, and `~/.gemini/tmp/aiproject/chats/` already exists as a registered project directory (proves the slug-registration half of the mechanism works) — but it's empty, no session was ever actually recorded there. `env -u all_proxy -u ALL_PROXY timeout 60 gemini -p "Say PONG"` hung and timed out with zero output: this environment's only proxy is SOCKS5-only (`all_proxy=socks5://127.0.0.1:9999`), and gemini-cli's bundled `undici` HTTP client can't use a SOCKS5 proxy (documented elsewhere as a known limitation for this exact CLI) — with the proxy unset there's no route to Google's API either. Still genuinely blocked on the user's own machine/terminal (outside this tool-execution sandbox's network constraints), not on anything fixable in code.
  - **Retried 2026-08-09 on Windows with Gemini CLI 0.53.0**: all proxy variables were unset, but `gemini -p "Reply with PONG only."` still produced no output within 120 seconds. The invocation registered the current worktree in `projects.json` but produced zero `chats/*.jsonl` files. A direct TCP probe resolved `generativelanguage.googleapis.com` but could not connect to port 443. The two timed-out Gemini Node processes were explicitly terminated.
- [ ] 4.6 Once a real `chats/*.jsonl` sample exists (from 4.5 or otherwise), diff it against the fixtures used in task 2.3 and adjust `aggregate_gemini_usage`/the fixtures if the real shape differs from what was read from source.
- [x] 4.7 **修复**（`openspec verify` 风格的独立复核，比照 `add-cli-memory-support` 那次发现的同类问题主动排查）：`specs/usage-statistics/spec.md` 的 `MODIFIED Requirements` 只列了"Normalized response usage records"的 7 个场景，但当前主 spec 该 requirement 下实际有 9 个场景——`Materialize provider log revisions`、`Preserve cache-only reported usage` 两个完全没出现在增量块里，会被真实归档器的"当前 spec 场景在增量块里找不到"防丢失检查拦下来（直接执行已安装 `@fission-ai/openspec` 包的 `buildUpdatedSpec` 复现确认）。另外发现一个更隐蔽的问题：`Persist reported tokens for an interactive terminal session` 这个场景名虽然保留了，但内容被整体重写成更简的版本，丢了当前主 spec 里 3 条 `AND` 子句（绑定 provider-native 会话身份、重复轮询更新同一条稳定记录而不是重复累加、持久化失败要通过统一日志上报）——这类"名字对得上、内容却悄悄丢guarantee"的问题，归档器不会报错，只能靠人/工具主动比对内容才能发现。修复：把两个缺失场景原样补回增量块，`Persist reported tokens for an interactive terminal session` 恢复那 3 条 `AND` 子句、只保留本次改动真正想要的措辞变化（"a supported CLI"→"any of the four supported CLIs (...)"）。用直接调用真实 `buildUpdatedSpec` 的方式确认修复后单变更应用成功（`+0 ~1 -0 ->0`），`openspec validate add-gemini-cli-terminal-usage-tracking --strict` 通过。
