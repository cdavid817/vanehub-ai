## 1. `tooling::mcp` domain and connection layer

- [x] 1.1 Add `ToolCallOutcome { content: String, is_error: bool }` to `tooling::mcp::domain::mod.rs`, sibling to `ConnectionOutcome`, with unit tests for its shape (mirroring `ConnectionOutcome`'s own test coverage style).
- [x] 1.2 Extend `McpConnectionPort` (`application/ports.rs`) with `async fn call_tool(&self, server: &ServerConfiguration, tool_name: &str, arguments: Value) -> ToolCallOutcome;`.
- [x] 1.3 Implement `RmcpConnectionAdapter::call_tool` (`infrastructure/connection_adapter.rs`), mirroring `test_inner`/`test_stdio`/`test_http`'s exact structure: build the same stdio/SSE transport, call `client.peer().call_tool(CallToolRequestParams::new(tool_name).with_arguments(arguments))` instead of `list_all_tools()`, wrap in the same timeout. `StreamableHttp` keeps erroring with the existing reserved-transport message.
- [x] 1.4 Add a `map_call_result` helper: join `ContentBlock::Text` blocks' text with newlines; render any non-text block as a short placeholder (e.g. `[image content omitted]`); treat `CallToolResult.is_error == Some(true)` as `ToolCallOutcome{is_error: true, ..}`; treat a `ServiceError` (connection/protocol failure) as `ToolCallOutcome{is_error: true, content: "MCP connection failed: ..."}`, matching `rmcp_error`'s existing message shape. **Also required a `_ => "[content omitted]"` wildcard arm** — `rmcp::model::ContentBlock` is `#[non_exhaustive]`, so an exhaustive-looking match over its 5 known variants still fails to compile without one.
- [x] 1.5 Renamed `MCP_TEST_TIMEOUT` to `MCP_OPERATION_TIMEOUT`; updated its doc comment.
- [x] 1.6 Added `tools/call` support to `src-tauri/tests/fixtures/mcp_stdio_server.cjs` — echoes `fixture_echo`'s `text` argument back as `echo: <text>`, and returns `isError: true` with a descriptive message for any other tool name (reused by 1.7's tool-level-error test, no second fixture needed).
- [x] 1.7 4 new connection-adapter tests: `stdio_fixture_calls_a_tool_and_returns_its_text_content`, `stdio_fixture_tool_call_reports_a_tool_level_error`, `call_tool_surfaces_connection_failure_as_an_error_outcome` (nonexistent command), `call_tool_total_timeout_returns_the_stable_outcome_shape`.

  Verified: `cargo test --lib tooling::mcp::` — 41 passed, 0 failed, 1 ignored (pre-existing, unrelated spawned-only fixture).

## 2. `tooling::mcp` application/facade layer

- [x] 2.1 Added `McpServerToolEntry { server_name: String, tool: ToolDescriptor }` to `application/models.rs`, re-exported from `application/mod.rs`.
- [x] 2.2 `McpApplicationService::visible_tool_catalog(&self, project_path: &str) -> Result<Vec<McpServerToolEntry>, McpApplicationError>` implemented exactly as planned.
- [x] 2.3 `McpApplicationService::call_tool(&self, project_path: &str, server_name: &str, tool_name: &str, arguments: Value) -> Result<ToolCallOutcome, McpApplicationError>` implemented exactly as planned (field name is `self.connection`, singular — not `self.connections` as tasks.md's draft assumed; corrected during implementation, no behavior difference).
- [x] 2.4 Added both as published `McpApi` methods (`api.rs`); re-exported `McpServerToolEntry`/`ToolCallOutcome` in its `pub(crate) use` list.
- [x] 2.5 5 new application-service tests: `visible_tool_catalog_includes_only_visible_active_servers_cached_tools` (active+tested included; inactive+tested and active+untested both excluded), `call_tool_rejects_a_server_outside_the_visible_active_set_without_connecting` (asserts the fake connection's `tool_calls` recorder stays empty), `call_tool_delegates_to_the_connection_port_for_a_visible_active_server`. Required extending two existing test doubles: `FakeConnection` gained a configurable `tool_call_outcome` and a `tool_calls` call recorder (kept `FakeConnection::new(outcome)` as a convenience constructor so the 3 pre-existing `test()`-only tests didn't need to change); `FakeRepository` gained a `statuses` map so `status()` can return configured cached tools instead of being hardcoded empty (falls back to the original hardcoded empty-tools `ServerStatus` when a name isn't seeded, so no pre-existing test's behavior changed).

  Verified: `cargo test --lib tooling::mcp::` — 41 passed, 0 failed, 1 ignored (same run as 1.7, both groups implemented together since `FakeConnection` needed the trait method before anything would compile).

## 3. `agent_runtime`: port, gateway adapter, and error variant

- [x] 3.1 Added `AgentToolCallOutcome { output: String, is_error: bool }` to `agent_runtime::application::models.rs`, sibling to `ToolDefinition`. **Also widened `ToolDefinition.name`/`.description` from `&'static str` to `String` in this same edit** (pulled forward from task 4.1 — both structs live in the same file and the doc comment on `AgentToolCallOutcome` needed to explain the relationship; fixed the resulting `tool_catalog()` literal-construction compile errors immediately, see 4.1's note).
- [x] 3.2 Added `AgentRuntimeApplicationError::Mcp(String)` (`application/error.rs`), mirroring `Skill`/`Memory`'s exact variant and `Display` shape. **Required updating 4 exhaustive `match` sites that don't compile without every variant covered**: `commands/error.rs` (grouped with `Skill`/`Memory` → `Self::storage`), `application/coordination.rs`'s `scheduler_error_classification` (grouped → `"persistence_or_runtime"`), `infrastructure/coordination_executor.rs`'s `classify_application_error` (grouped → `NonRetryable` — correct, not just convenient: `Mcp` is only ever constructed from `catalog_entries`'s lookup failure per 3.3/3.4, never from a live tool call, so it's a lookup/config-style failure like `Skill`/`Memory`, not an execution-style one), `sessions/infrastructure/runtime_support.rs`'s `agent_runtime_error` (grouped → `SessionsApplicationError::Runtime`).
- [x] 3.3 Added `AgentMcpToolPort: Send + Sync` to `application/ports.rs` exactly as planned.
- [x] 3.4 New `agent_runtime::infrastructure::mcp_tool_gateway.rs`: `RuntimeAgentMcpToolAdapter` implemented exactly as planned, with the name-split/field-defaulting logic factored into two pure, directly-testable functions (`split_tool_name`, `to_tool_definition`) rather than left inline in the trait impl.
- [x] 3.5 5 tests for the two pure functions (`split_tool_name`: normal split, a tool name containing its own `__`, a non-prefixed name; `to_tool_definition`: field-defaulting, field-preservation). **Scope note**: `skill_gateway.rs` (the precedent this file mirrors) has zero tests of its own — its whole file is a thin delegate-and-reshape wrapper with no test module. Following that same precedent, the trait-impl methods themselves (`catalog_entries`/`call_tool`) are left untested at this file's level once their extractable logic was pulled into the two pure functions above; the full dispatch behavior (including a fake port's `Err`-becomes-`is_error:true` path) is covered instead at `execute()`'s own test level in section 4, using a fake `AgentMcpToolPort` — consistent with how `AgentSkillPort`'s behavior is actually exercised in this codebase (through `api_process_adapter.rs`'s tests, never through `skill_gateway.rs`'s own).

  Verified: `cargo check --lib` (0 errors — dead-code-only warnings expected until section 4 wires the adapter into `RuntimeAgentApiAdapter`); `cargo test --lib mcp_tool_gateway` — 5 passed, 0 failed.

## 4. `agent_runtime`: tool catalog and dispatch integration

- [ ] 4.1 Widen `ToolDefinition.name`/`.description` from `&'static str` to `String` (`application/models.rs`); update `tool_catalog()`'s three literal constructions with `.to_string()`; confirm `risk_tier_for`, `build_request_body` (both wire formats), and every existing test still compile against owned `String` (expected: no further changes, since all existing consumers already operate through `&str`/JSON serialization).
- [x] 4.2 Factored the merge into its own `resolve_tool_catalog(request, mcp, logging, clock) -> Vec<ToolDefinition>` function, called from `execute()` as `let tools = resolve_tool_catalog(request, mcp, logging, clock);` — mirrors `resolve_system_prompt` being its own top-level function rather than inlined, specifically so 4.8's test can exercise the merge/degrade-gracefully behavior directly without going through `execute()`'s full network-calling path (there is no HTTP mock server in this test file, and every existing `execute()`-level test already stops short of the real network call for the same reason).
- [x] 4.3 Threaded `mcp: &dyn AgentMcpToolPort` through `execute()`'s signature and into `execute_tool_call`, plus through `run_generation`/`monitor_generation`'s `Arc` clone-and-thread chain (not explicitly called out in the original task text, but required to actually reach `execute()` from `RuntimeAgentApiAdapter`).
- [x] 4.4 Added the MCP dispatch branch in `execute_tool_call`, exactly as planned (before the `workspace_folder` gate, alongside `remember`).
- [x] 4.5 Updated `RuntimeAgentApiAdapter` struct/`new(...)` to carry `mcp: Arc<dyn AgentMcpToolPort>`.
- [x] 4.6 Added `mcp_sourced_tool_names_always_require_approval` to `tool_catalog.rs` — confirms zero production code change was needed, as predicted.
- [x] 4.7 Added `FakeMcp` (configurable catalog/call-outcome test double, distinct from the simpler always-erroring `NoopMcp` used everywhere else) plus 2 tests: `execute_tool_call_routes_mcp_prefixed_names_to_the_mcp_port_and_maps_the_outcome`, `execute_tool_call_routes_mcp_calls_even_without_a_workspace_folder` (asserts the port receives `""` as `project_path`, confirming `unwrap_or_default()` behaves as designed).
- [x] 4.8 2 tests directly against the new `resolve_tool_catalog` (see 4.2's note on why this is more directly testable than going through `execute()`): on success, the 3 fixed entries plus every returned MCP entry are all present; on failure, falls back to exactly the 3 fixed entries and logs exactly one `Warn`-level `session.runtime.api.mcp` log containing the underlying error message.

  Verified: `cargo check --lib --tests` (0 warnings — this was also the point where all "never used" dead-code warnings from sections 1-3 disappeared, confirming the new MCP path is genuinely wired end to end, not just independently compiling); `cargo test --lib api_process_adapter` — 48 passed, 0 failed (up from 41 pre-existing).

## 5. Bootstrap wiring

- [x] 5.1 Added `mcp: McpApi` to `AgentRuntimeDependencies` (`bootstrap/agent_runtime.rs`).
- [x] 5.2 In `assemble_agent_runtime_api`, constructed `agent_mcp_tools` and passed it into `RuntimeAgentApiAdapter::new(...)`, exactly as planned.
- [x] 5.3 In `bootstrap/runtime.rs`, passed `mcp: mcp_api.clone()` into the `AgentRuntimeDependencies { .. }` literal — `mcp_api` was indeed already constructed earlier in that function, no reordering needed. **Done ahead of its natural position** in this task list because `bootstrap/agent_runtime.rs`'s existing `RuntimeAgentApiAdapter::new(...)` call site is a hard compile dependency the moment `RuntimeAgentApiAdapter`'s constructor gains a new required parameter (section 4.5) — the crate would not compile with section 5 left for later.

  Verified: `cargo check --lib` (0 errors, 0 warnings) — same run as section 4's verification, since both were required together to reach a compiling state.

## 6. Frontend Web/mock parity

- [x] 6.1 Added a simulated `mcp__mock-server__search` tool call to `web-agent-client.ts`'s API-agent tool-call sequence, reusing the *exact same* `pendingMockToolApprovals`/`resolveToolApproval` approval-gated flow `shell` already uses (not `remember`'s immediate-completed shape) — correct choice, since the real backend classifies MCP-sourced calls `RequiresApproval` unconditionally, matching `shell`'s approval requirement, not `remember`'s auto-approve.
- [x] 6.2 1 new test, `simulates an MCP-sourced tool call that requires approval before completing`. **Scope note**: no existing test in this file covered `shell`'s awaiting_approval → `resolveToolApproval` round trip either (only `remember`'s simpler immediate-completed shape had coverage, at the test now directly above this one) — rather than leave the new MCP path as equally uncovered, this test exercises the full round trip (awaiting_approval event appears → `resolveToolApproval` resolves it → completed event appears with the same call id), which is more thorough than the precedent it's "matching," not less.

  Verified: `npx vitest run src/services/web-agent-client.test.ts` — 45 passed (up from 44), 0 failed. `npx tsc --noEmit` — clean.

## 7. Verification

- [x] 7.1 `cargo test` (full, unscoped) — **924 passed, 0 failed, 3 ignored** (up from the 906-passed baseline at the start of this change; the 3 ignored are the same pre-existing spawned-only fixtures, unrelated to this change).
- [x] 7.2 `cargo clippy --lib --bins --tests --manifest-path src-tauri/Cargo.toml` — 0 warnings. **One fix needed along the way**: `RuntimeAgentApiAdapter::new` crossed clippy's default 7-argument threshold once `mcp` was added as its 8th parameter — added `#[allow(clippy::too_many_arguments)]`, matching the same attribute already present on `execute`/`execute_tool_call`/`run_generation` in this same file for the identical reason.
- [x] 7.3 `cargo fmt --check --manifest-path src-tauri/Cargo.toml` — no diff (ran `cargo fmt` once to normalize import-list wrapping and a few multi-line call sites introduced across sections 1-4, then confirmed `--check` is clean; re-ran `cargo test`/`cargo clippy` afterward to confirm formatting made no behavioral difference).
- [x] 7.4 `npm run test` — **97 files, 330 tests passed** (up from 97/329).
- [x] 7.5 `npm run lint` and `npx tsc --noEmit` — both clean.
- [x] 7.6 `npm run build` — clean (same pre-existing large-chunk-size warning noted in prior phases, unrelated to this change).
- [x] 7.7 `openspec validate add-agent-mcp-tools --strict` — valid.
- [ ] 7.8 Manual smoke test (desktop, a real or locally-run MCP server configured and active for a test project): confirm a native API agent's tool catalog includes that server's tools; approve and run an MCP tool call and confirm the result reaches the model; deny an MCP tool call and confirm no connection is attempted; disable the server and confirm its tools disappear from the next generation's catalog. **Deferred to the user** (manual/credential-requiring verification, same standing arrangement as prior native-agent phases).
