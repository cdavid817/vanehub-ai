{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default VaneHub AI desktop capability. Local privileged operations are exposed through declared Tauri commands and frontend service adapters.",
  "windows": ["main", "floating-assistant"],
  "permissions": ["core:default", "dialog:allow-open"]
}
