//! Use cases and ports coordinating semantic code-intelligence requests.

// Bootstrap assembly is introduced after the persistence foundation in the task sequence.
#[cfg_attr(not(test), allow(dead_code))]
pub(crate) mod ports;
