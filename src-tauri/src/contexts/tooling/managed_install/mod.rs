//! Acquiring an artifact VaneHub will then run or unpack.
//!
//! The one audited download path, shared rather than copied. A second implementation of the
//! allowlist, the redirect loop, or the byte ceiling would drift, and none of the ways it drifts
//! are visible when reviewing the copy on its own.
//!
//! Contributors declare *what* they are fetching and under what bounds; this subdomain enforces
//! the bounds. It knows nothing about CLI tools or language servers.

pub(crate) mod api;
pub(crate) mod domain;
pub(crate) mod infrastructure;
